name: Agent Verifier

# Manual trigger only - run locally by default to save API costs
# Use: Actions > Agent Verifier > Run workflow
# Or: gh workflow run agent_verifier.yml
on:
  workflow_dispatch:
    inputs:
      files:
        description: 'Files to verify (space-separated, or leave empty for all)'
        required: false
        default: ''
      pr_number:
        description: 'PR number to comment on (optional)'
        required: false
        default: ''

permissions:
  contents: read
  pull-requests: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Set up Node.js (required for Claude Agent SDK)
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          pip install claude-agent-sdk
          pip install ".[test]"

      - name: Determine files to check
        id: files
        run: |
          if [ -n "${{ inputs.files }}" ]; then
            echo "files=${{ inputs.files }}" >> $GITHUB_OUTPUT
          else
            # Default: all Python files in core directories
            FILES=$(find rcx_pi prototypes tests -name "*.py" 2>/dev/null | tr '\n' ' ')
            echo "files=$FILES" >> $GITHUB_OUTPUT
          fi

      - name: Run verifier agent
        id: verify
        if: steps.files.outputs.files != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python tools/run_verifier.py ${{ steps.files.outputs.files }} 2>&1 | tee /tmp/verifier_output.txt
          echo "exit_code=${PIPESTATUS[0]}" >> $GITHUB_OUTPUT

      - name: Post PR comment
        if: always() && inputs.pr_number != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f /tmp/verifier_output.txt ]; then
            REPORT=$(cat /tmp/verifier_output.txt | sed -n '/^====/,/^====/p' | tail -n +2 | head -n -1)

            if [ -z "$REPORT" ]; then
              REPORT=$(tail -50 /tmp/verifier_output.txt)
            fi

            if [ "${{ steps.verify.outputs.exit_code }}" = "0" ]; then
              STATUS="✅ **VERIFIER PASSED**"
            else
              STATUS="❌ **VERIFIER FAILED**"
            fi

            gh pr comment ${{ inputs.pr_number }} --body "## 🔍 Verifier Agent Report

$STATUS

<details>
<summary>Full Report</summary>

\`\`\`
$REPORT
\`\`\`

</details>

---
*Manual review by RCX Verifier Agent*"
          fi

      - name: Summary
        if: always()
        run: |
          echo "## Verifier Results" >> $GITHUB_STEP_SUMMARY
          if [ -f /tmp/verifier_output.txt ]; then
            echo '```' >> $GITHUB_STEP_SUMMARY
            tail -50 /tmp/verifier_output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
