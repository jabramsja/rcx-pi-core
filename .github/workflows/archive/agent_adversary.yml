name: Agent Adversary

# Manual trigger only - run locally by default to save API costs
# Use: Actions > Agent Adversary > Run workflow
# Or: gh workflow run agent_adversary.yml

concurrency:
  group: agent-adversary-${{ github.run_id }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      files:
        description: 'Files to attack (space-separated, or leave empty for all)'
        required: false
        default: ''
      pr_number:
        description: 'PR number to comment on (optional)'
        required: false
        default: ''

permissions:
  contents: read
  pull-requests: write

jobs:
  attack:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Set up Node.js (required for Claude Agent SDK)
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          pip install claude-agent-sdk
          pip install ".[test]"

      - name: Determine files to check
        id: files
        run: |
          if [ -n "${{ inputs.files }}" ]; then
            echo "files=${{ inputs.files }}" >> $GITHUB_OUTPUT
          else
            # Default: all Python files in core directories
            FILES=$(find rcx_pi prototypes tests -name "*.py" 2>/dev/null | tr '\n' ' ')
            echo "files=$FILES" >> $GITHUB_OUTPUT
          fi

      - name: Run adversary agent
        id: attack
        if: steps.files.outputs.files != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PYTHONHASHSEED: "0"
        run: |
          python tools/run_adversary.py ${{ steps.files.outputs.files }} 2>&1 | tee /tmp/adversary_output.txt
          echo "exit_code=${PIPESTATUS[0]}" >> $GITHUB_OUTPUT

      - name: Post PR comment
        if: always() && inputs.pr_number != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f /tmp/adversary_output.txt ]; then
            REPORT=$(cat /tmp/adversary_output.txt | sed -n '/^====/,/^====/p' | tail -n +2 | head -n -1)

            if [ -z "$REPORT" ]; then
              REPORT=$(tail -50 /tmp/adversary_output.txt)
            fi

            if [ "${{ steps.attack.outputs.exit_code }}" = "0" ]; then
              STATUS="✅ **ADVERSARY PASSED** (no critical vulnerabilities)"
            else
              STATUS="⚠️ **ADVERSARY FOUND ISSUES** (review required)"
            fi

            gh pr comment ${{ inputs.pr_number }} --body "## 🔴 Adversary Agent Report

$STATUS

<details>
<summary>Full Report</summary>

\`\`\`
$REPORT
\`\`\`

</details>

---
*Manual red team review by RCX Adversary Agent*"
          fi

      - name: Summary
        if: always()
        run: |
          echo "## Adversary Results" >> $GITHUB_STEP_SUMMARY
          if [ -f /tmp/adversary_output.txt ]; then
            echo '```' >> $GITHUB_STEP_SUMMARY
            tail -50 /tmp/adversary_output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
