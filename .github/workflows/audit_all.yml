name: Required Audit Checks

# Manual-only required-check audit with optional stress evidence.
# NOTE: green_gate.yml now includes anti-cheat scans and fixture validation
# This workflow adds: CLI exec-summary spot-check, repo clean verification
# This is not fail-closed release/stress proof; weekly_deep_fuzz.yml owns that lane.
on:
  workflow_dispatch:  # Manual required-check trigger; avoids duplicate runs with green_gate.yml

concurrency:
  group: audit-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONHASHSEED: "0"
      PYTHONPATH: "."
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.13"
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: Install package + test deps
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -U pip
          python -m pip install -e ".[test]"

      - name: Run required audit checks
        shell: bash
        run: |
          set -euo pipefail
          tools/audit_all.sh
