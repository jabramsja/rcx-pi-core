name: RCX Replay Fixture Gate

on:
  pull_request:
  push:

jobs:
  replay-fixture-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Replay fixture gate
        run: ./scripts/check_replay_fixture.sh
