{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RCX Engine Run Schema v1",
  "description": "Raw engine execution artifact (opaque, pre-normalization). We only assert it is a JSON object and optionally carries a schema_version.",
  "type": "object",

  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["rcx.engine_run.v1"]
    }
  },

  "additionalProperties": true
}
