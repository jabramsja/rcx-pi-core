{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "rcx-omega://schemas/omega_summary.v1.schema.json",
  "title": "RCX-Ω Omega-summary JSON (v1)",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "schema_version": {
      "description": "Optional semantic version of the schema governing this payload.",
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "kind": {
      "description": "Optional discriminator for payload family.",
      "type": "string",
      "enum": ["omega_summary"]
    },
    "result": {
      "description": "Required summary payload. Shape is implementation-defined but must be JSON-serializable.",
      "type": ["object", "array", "string", "number", "boolean", "null"]
    },
    "steps": {
      "description": "Optional; only present when producer chooses to embed trace steps (typically via --trace).",
      "type": "array",
      "items": { "type": "object", "additionalProperties": true }
    },
    "stats": {
      "description": "Optional; if steps are present, stats may also be present.",
      "type": "object",
      "additionalProperties": true
    }
  },
  "required": ["result"]
}
