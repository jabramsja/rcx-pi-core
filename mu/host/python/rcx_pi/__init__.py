# rcx_pi/__init__.py
"""
RCX-π package — L3 self-hosting kernel.

Active API surface lives in rcx_pi.selfhost.* (canonical) with
re-export shims at rcx_pi.eval_seed, rcx_pi.mu_type, etc.

Legacy Motif-based evaluator, programs, and CLI chain have been
archived to archive/rcx_pi_legacy/.
"""
