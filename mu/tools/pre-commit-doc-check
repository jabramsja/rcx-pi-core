#!/usr/bin/env bash
# Backward-compat wrapper — canonical location: tools/hooks/pre-commit-doc-check
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || python3 -c "import os,sys;print(os.path.realpath(sys.argv[1]))" "$0")")" && pwd)"
exec "$SCRIPT_DIR/hooks/pre-commit-doc-check" "$@"
