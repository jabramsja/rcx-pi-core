# Deferred Non-Blocking Findings: codex-startup-hardening-2026-04-14

Wave: codex-startup-hardening-2026-04-14
Class: MAINTENANCE
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 0 active finding(s) (11 resolved in this wave).

This artifact is retained as provenance for the prior review cycle. The staged
startup-hardening wave now resolves the previously reported non-blocking issues,
and no active advisory residue remains in the current staged set.

## Resolved in this wave
- ~~Shared-learning snapshot drops the date from `FIXED` learning entries~~ fixed in `mu/tools/session/founder_learning_snapshot.py` and covered by the staged startup-state validation surface.
- ~~Preflight learning-date snippet uses GNU-only `grep -P`~~ replaced with BSD/macOS-portable extraction in `.claude/skills/preflight/SKILL.md`.
- ~~Pane 1 idle copy still claims a 5-minute window~~ updated to one-hour wording in `mu/tools/observability/pipeline_monitor.sh`.
- ~~The governing startup packet still reflects pre-rerun metadata and stale TASKS line references~~ synchronized to the active `[CODEX-STARTUP-HARDENING]` authorization and current Phase B state.
- ~~The deferred non-blocker artifact still claims the default.rules alias-bypass defect~~ resolved by the staged `mu/tools/session/check_codex_startup_state.py` logic and regression coverage.
- ~~Governing packet still advertises pre-rerun Phase A state~~ header/state updated to current Phase B governance truth.
- ~~Tracker sync note underreports the live staged scope and validation surface~~ `TASKS.md` now records the 20-file wave-owned scope and 5 wave-owned pytest modules.
- ~~Resolved non-blocker packet incorrectly says the governing packet metadata was fixed~~ this artifact now records resolved provenance instead of stale live findings.
- ~~The governing packet still says this wave is blocked for rerun~~ self-blocking re-entry language removed; the next action is a fresh bounded meta review from synchronized staged truth.
- ~~The staged tracker sync note underreports the current validation surface~~ evidence command and file-count claim are aligned with the staged diff.
- ~~The deferred non-blocker packet still lists several findings that are already resolved in the staged code~~ this artifact now carries only the resolved summary above.
