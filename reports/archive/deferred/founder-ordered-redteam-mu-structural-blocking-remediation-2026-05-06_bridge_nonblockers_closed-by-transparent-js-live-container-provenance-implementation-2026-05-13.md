# Deferred Non-Blocking Findings: founder-ordered-redteam-mu-structural-blocking-remediation-2026-05-06

Archived: 2026-05-13
Closed by: transparent-js-live-container-provenance-implementation-2026-05-13
Source file: `reports/deferred/non_blocking/founder-ordered-redteam-mu-structural-blocking-remediation-2026-05-06_bridge_nonblockers.md`
Status: CLOSED_ARCHIVED

## Closure Evidence

- Direct transparent Proxy probe exits 0 with transparent record/list Proxy values rejected at public JS Mu boundaries and trusted Mu-origin records accepted.
- Commit executor supervisor Step 6 rejected the initial raw WeakSet export because `_TRUSTED_MU_CONTAINERS.add(proxy)` made transparent Proxy roots pass validation/hash; the next supervisor pass rejected the global-symbol WeakSet export because `types[Symbol.for('rcx.mu.internalProvenance')].add(proxy)` had the same effect; the next supervisor pass rejected the exported recursive `containers.json` copy helper because it laundered raw records/lists and transparent Proxy wrappers into valid/hashable Mu containers; the next supervisor rejection proved public `containers.record(Object.entries(proxy))` and `containers.list(proxyArray)` were still laundering paths; the final supervisor rejection found `normalizeProjection` and `deriveEngineExitReason` still constructing plain compound records inside scoped producer paths. The repaired closeout confines constructors to repo-internal `container_factory.js`, exposes public `types.js` `containers` as `["has"]` only, converts the remaining scoped producers to factory-backed records, exposes no global-symbol/raw WeakSet/string-named trust mutator, `.add()` capability, exported `list`/`record` constructor, or recursive host-object/Proxy copy helper from `types.js`, and requires provenance on every compound node.
- `node mu/host/js/eval_step.js` exits 0 and reports `All tests passed: true`.
- `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/l4_gates/test_d009_production_depth_gate.py --tb=short` exits 0 with `44 passed`.
- `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/parity/test_js_parity_automated.py --tb=short -p no:cacheprovider` exits 0 with `307 passed in 484.69s (0:08:04)`.
- Focused JS parity checks for empty-var matching, JSON recurrence input, and JS security parity exit 0 after test fixtures trust only explicitly Mu-origin compound values.
- `python3 mu/tools/checks/check_host_semantics_ratchet.py --json` exits 0 with `"passed": true`.
- `python3 tools/checks/check_host_authority_inventory_ratchet.py` exits 0 with no new total-inventory or authority-subset sites detected; current JS total inventory is one site below baseline and the authority subset is flat.

## Original Retained Finding

Wave: founder-ordered-redteam-mu-structural-blocking-remediation-2026-05-06
Class: L4_STRUCTURAL
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 1 retained finding(s).

2026-05-10 triage evidence refresh:

- Current JS validation still relied on host reflective shape checks in both
  validation paths: `mu/host/js/core/types.js:116` through
  `mu/host/js/core/types.js:158` and `mu/host/js/core/types.js:176` through
  `mu/host/js/core/types.js:220` wrapped prototype/key/descriptor inspection in
  fail-closed `try` blocks.
- Direct transparent Proxy probe exited 0 with `plainValid:true`,
  `proxyValid:true`, and successful `muHashCached(transparentProxy)`, proving
  transparent Proxy rejection was not code-closed by then-current structural
  reflection.
- `TASKS.md` recorded the blocking remediation as merged by PR #912 and
  explicitly preserved only hostile/trapping Proxy coverage; transparent Proxy
  rejection still required a structural Mu provenance rule or an explicit
  host-oracle authorization.
- Current governing route:
  `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`
  as a separate bounded `/mu` structural packet.

2026-05-11 reconciliation:

- **Outcome:** retained live advisory.
- **Governing route:** `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`.
- **Current proof gap:** the routed Phase A packet selected structural
  provenance / boundary normalization, but no successor implementation packet
  had been locked. Current JS still accepted transparent `new Proxy({a: 1}, {})`
  values through validation and hashing as plain Mu records.
- **Hard stop before implementation:** this deferred packet and the completed
  Phase A policy packet authorized no runtime, hash, validator, Stage0, seed,
  scheduler, registry, parity, or production `/mu` edits. A successor packet
  had to enumerate exact implementation files and validation.
- **Doctrine boundary:** future work had to narrow host bootstrap debt through
  Mu-aligned structural provenance or boundary normalization; it had to avoid
  semantic host debt through `util.types.isProxy` or an equivalent JavaScript
  host-object oracle without explicit founder authorization.

### Transparent JavaScript Proxy rejection needs a structural provenance rule or explicit host-oracle override

- **Class:** POLICY_BOUND
- **Severity:** medium
- **File:** mu/host/js/core/types.js
- **Disposition:** non_blocking
- **Evidence:** pre-push purity gate rejected the same-wave `require('util')`
  proxy-oracle import as a new JavaScript kernel stdlib import; pure structural
  reflection could not distinguish `new Proxy({a: 1}, {})` from its target
  without invoking a host detector.
- **Boundary:** hostile/trapping proxy inspection failed closed in the active
  blocking wave. Fully transparent proxy rejection was deferred until a
  structural Mu provenance boundary or explicit founder override authorized a
  host oracle.
- **Governing route:** `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`
