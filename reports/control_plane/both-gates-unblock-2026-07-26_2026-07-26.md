# Both-Gates-Unblock-2026-07-26

Date: 2026-07-26
Status: Phase B (locked, implementing)
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: both-gates-unblock-2026-07-26
Phase-A-Lock: LOCKED
Purpose: Unblock the repository. THREE pre-push/CI blockers are repaired here; two of them are mutually interlocked and cannot land separately, and the third was surfaced by the pipeline's own recovery while this package was being pushed.

## Scope

Carry three verified pre-push/CI gate repairs in one package; two are interlocked.

## Request from Post-Merge Supervisor

Unblock the repository. THREE pre-push/CI blockers are repaired here; two of them are mutually interlocked and cannot land separately, and the third was surfaced by the pipeline's own recovery while this package was being pushed.

GATE 1 - theater risk ratchet: 10 founder allowlist entries had lapsed, so the checker exits 1. It is invoked by both the CI green gate and the pre-push fast audit, so no wave could push and no PR could pass CI. Repaired STRUCTURALLY, not by extending any expiry date: two raises-on-failure validators registered in the classifier proof-class registry, real evidence-integrity assertions added to five observational profiling tests that previously had none, and the allowlist pruned from 84 entries to 9 via the checker's own update mode.

GATE 2 - stale counter-proof: an assertion required a leaked pager route override to break a reproduced target, proving the leak is real, before verifying the validation child strips it. The target had drifted and passed with AND without the override, so the assertion was unsatisfiable. That single red test also poisons the Gate-10 receipt-chain attestation, because the supervisor shells out to this exact test file whenever a wave stages a receipt-chain file - making every control-plane wave unlandable regardless of implementation quality. Re-pointed; the gate remains a real gate and the loud-failure-never-silent-skip property is preserved.

THE INTERLOCK between gates 1 and 2, which is why they ship together: a branch carrying only the theater fix fails pre-push on the counter-proof test; a branch carrying only the counter-proof fix fails pre-push on the theater ratchet. Each repairs exactly the gate that blocks the other.

GATE 3 - incomplete one-shot timeout hardening (completes an already-landed commit). Commit 0afe5a2b 'test: harden pane process one-shot timeout' is an ancestor of this branch. It introduced the module constant _OBSERVABILITY_ONESHOT_TIMEOUT_S, default 30s and overridable via RCX_TEST_OBSERVABILITY_ONESHOT_TIMEOUT_S, and converted the observability one-shot subprocess call sites to use it - but MISSED one straggler, which kept a raw timeout=10. Under the parallel xdist load of the full pre-push suite that straggler's bash child is SIGKILLed at 10s (returncode -9), so pre-push reported '1 failed, 8330 passed' and no wave could push. The same test completes in 0.49s when run alone, so this is contention headroom, not test slowness. The single-line repair points the straggler at the constant its sibling call sites already use. No assertion is weakened, skipped, xfailed or deleted; all seven assertions in that test are untouched.

Constraints: no assertion was weakened, skipped, xfailed or deleted; no expiry date was extended; no runtime substrate, executor, recovery or control-surface file is touched.

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_TYPED_FAIL_CLOSED_OUTCOMES.
- `indicator_artifact_ref`: reports/l4_wave_indicators/both-gates-unblock-2026-07-26.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id both-gates-unblock-2026-07-26 --output reports/l4_wave_indicators/both-gates-unblock-2026-07-26.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_commit_executor_receipt.py mu/tests/tools/test_check_gate_behavioral_pairs.py mu/tests/tools/test_check_theater_risk_ratchet.py && python3 tools/checks/check_theater_risk_ratchet.py`.
- `evidence_delta`: Before: the theater ratchet exits 1 with 10 expired entries, the counter-proof assertion fails, and the full pre-push suite reports 1 failed / 8330 passed on a one-shot timeout straggler, so pre-push and the CI green gate are red for every wave. After: the ratchet exits 0 from both paths with classifier and allowlist counts at 9, the counter-proof passes for a real reason, the straggler uses the same constant as its sibling call sites, and pre-push runs clean..
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: ADVANCE.
- `founder_override`: both-gates-unblock-2026-07-26.
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `both-gates-unblock-2026-07-26`
- Active packet: `reports/control_plane/both-gates-unblock-2026-07-26_2026-07-26.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `ca5482adf9ff6d94690e244c11ab7950327f0f375cbaee6ceac6ffb89ce293e5`
- Indicator artifact: `reports/l4_wave_indicators/both-gates-unblock-2026-07-26.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_commit_executor_receipt.py mu/tests/tools/test_check_gate_behavioral_pairs.py mu/tests/tools/test_check_theater_risk_ratchet.py && python3 tools/checks/check_theater_risk_ratchet.py`.
- Evidence delta: Before: the theater ratchet exits 1 with 10 expired entries, the counter-proof assertion fails, and the full pre-push suite reports 1 failed / 8330 passed on a one-shot timeout straggler, so pre-push and the CI green gate are red for every wave. After: the ratchet exits 0 from both paths with classifier and allowlist counts at 9, the counter-proof passes for a real reason, the straggler uses the same constant as its sibling call sites, and pre-push runs clean..
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/both-gates-unblock-2026-07-26.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_recovery_gate.py`
  - `reports/control_plane/both-gates-unblock-2026-07-26_2026-07-26.md`
  - `reports/l4_wave_indicators/both-gates-unblock-2026-07-26.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
