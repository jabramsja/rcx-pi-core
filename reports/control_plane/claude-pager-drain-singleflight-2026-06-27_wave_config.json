{
  "wave_id": "claude-pager-drain-singleflight-2026-06-27",
  "title": "Claude Pager Drain Singleflight 2026-06-27",
  "task_id": "[NEXT-CODEX-POST-QUEUE-CLEANUP]",
  "purpose": "Fix the stale Claude pager receiver herd as a structural control-plane cleanup before advancing the remaining autonomous program queue. Make claude_pager_receiver ensure_draining single-flight for a repo/bus drain so repeated enqueue attempts cannot spawn hundreds of detached --once drainers behind the delivery lock. Preserve queued event durability, fail-open pager semantics, delivery-lock serialization, Codex route defaults, and commit-executor ownership. Also create a launcher-compatible Coinduction config artifact so the next math-structure item can be launched by the pipeline instead of by manual handoff.",
  "date": "2026-06-27",
  "wave_class": "L4_ENABLER",
  "target_gate_id": "G8",
  "primary_blocker_class": "INTEGRATION",
  "primary_invariant_id": "INV_STRUCTURAL_FORWARD_MOTION",
  "indicator_artifact_ref": "reports/l4_wave_indicators/claude-pager-drain-singleflight-2026-06-27.json",
  "indicator_collection_command": "python3 tools/metrics/collect_l4_wave_indicators.py --wave-id claude-pager-drain-singleflight-2026-06-27 --output reports/l4_wave_indicators/claude-pager-drain-singleflight-2026-06-27.json",
  "implementer_agent": "codex",
  "reviewer_agent": "codex",
  "pager_route": "codex",
  "evidence_command": "python3 -m py_compile mu/tools/session/claude_pager_receiver.py mu/tools/observability/pipeline_agent_pager.py && PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_claude_pager_receiver.py mu/tests/tools/test_pipeline_agent_pager.py --tb=short && ./tools/checks/check_docs_consistency.sh && python3 tools/checks/enforce_l4_execution_contract.py --staged --wave-id claude-pager-drain-singleflight-2026-06-27 --wave-class L4_ENABLER && git diff --check",
  "evidence_delta": "(1) claude_pager_receiver ensure_draining becomes single-flight for detached --once drains so repeated pager events cannot create a stale process herd while credentials or delivery are unhealthy. (2) Focused tests prove repeated drain ensures reuse or report the already-live drainer instead of spawning duplicates. (3) A launcher-compatible Coinduction config exists for the next PROGRAM QUEUE item so post-merge routing can proceed through launch_wave.py rather than manual receipts or handoff.",
  "progress_proof_before": "After PR #1162 landed, the active lane is clean and the remaining PROGRAM QUEUE next item is Coinduction. The stale pager cleanup found 1207 orphaned claude_pager_receiver --once processes and 17 stale queued events caused by repeated failed Claude pager delivery. Code inspection shows ensure_draining spawns a detached --once receiver on every call while delivery.lock only serializes actual delivery, so failed delivery can accumulate a process herd.",
  "progress_proof_after": "Repeated Claude pager enqueue/drain attempts are bounded by a single live detached drainer per repo/bus, stale drainer pid state is safely refreshed, focused tests cover the regression, and the next Coinduction wave has a launcher config ready for pipeline execution.",
  "structural_artifact_ref": "mu/tools/session/claude_pager_receiver.py; mu/tests/tools/test_claude_pager_receiver.py; mu/tests/tools/test_pipeline_agent_pager.py; reports/control_plane/coinduction-non-termination-as-structure-2026-06-27_wave_config.json; reports/l4_wave_indicators/claude-pager-drain-singleflight-2026-06-27.json",
  "post_gate_contract_sweep": "python3 -m py_compile mu/tools/session/claude_pager_receiver.py mu/tools/observability/pipeline_agent_pager.py && PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_claude_pager_receiver.py mu/tests/tools/test_pipeline_agent_pager.py --tb=short && ./tools/checks/check_docs_consistency.sh && python3 tools/checks/enforce_l4_execution_contract.py --staged --wave-id claude-pager-drain-singleflight-2026-06-27 --wave-class L4_ENABLER && git diff --check",
  "scope_summary": "Control-plane cleanup only. Fix claude_pager_receiver detached drainer spawning so pager failures cannot create another stale process herd, update focused tests, and create the next Coinduction launcher config. Do not implement Coinduction, Fixpoint, Optimization, runtime semantics, substrate semantics, seed semantics, projection semantics, tmux layout changes, or manual commit/PR work in this wave.",
  "scope_items": [
    "reports/control_plane/claude-pager-drain-singleflight-2026-06-27_wave_config.json (KEEP) -- launcher config for this cleanup wave.",
    "mu/tools/session/claude_pager_receiver.py (MODIFY) -- make ensure_draining single-flight for detached --once drains per repo/bus and safely refresh stale pid state.",
    "mu/tests/tools/test_claude_pager_receiver.py (MODIFY) -- update the existing repeatability regression and add coverage for duplicate drain ensures under persistent failure conditions.",
    "mu/tests/tools/test_pipeline_agent_pager.py (MODIFY IF NEEDED) -- keep pipeline pager enqueue/ensure contract aligned if public return shape changes.",
    "reports/control_plane/coinduction-non-termination-as-structure-2026-06-27_wave_config.json (CREATE) -- launcher-compatible config for the next PROGRAM QUEUE item only; do not implement Coinduction in this cleanup wave.",
    "TASKS.md (MODIFY THROUGH LAUNCHER TRACKER SYNC ONLY) -- tracker sync after the cleanup package lands, preserving Coinduction as the next math-structure item.",
    "STATUS.md (MODIFY ONLY IF NEEDED) -- current control-plane cleanup and queue wording only if mechanically required.",
    "reports/control_plane/claude-pager-drain-singleflight-2026-06-27_2026-06-27.md (GENERATED BY LAUNCHER) -- launcher-created packet for this wave.",
    "reports/l4_wave_indicators/claude-pager-drain-singleflight-2026-06-27.json (GENERATED BY WAVE) -- indicator artifact for this wave."
  ],
  "work_items": [
    "Read the current claude_pager_receiver ensure_draining and run_until_empty behavior and the pipeline_agent_pager _dispatch_claude enqueue/ensure contract before editing.",
    "Implement a single-flight detached drainer guard for the same repo/bus drain path, using durable pid or equivalent process state under the receiver observability directory and a race-safe lock around spawn decisions.",
    "Treat missing, dead, stale, or command-mismatched drainer state as refreshable; treat a matching live drainer as already draining and return a non-error accepted result without spawning another process.",
    "Preserve event durability: queued JSON files must not be dropped, rewritten as delivered, or hidden because a duplicate drain ensure was skipped.",
    "Preserve delivery serialization: delivery.lock or equivalent must still guard actual Claude delivery so only one drainer performs delivery work at a time.",
    "Preserve fail-open pager behavior for pipeline callers: enqueue and duplicate-drainer detection must not make pipeline execution fail because Claude credentials are unhealthy.",
    "Update focused tests so repeated ensure_draining calls no longer expect multiple Popen calls while still proving a fresh drainer can be started after stale/dead drainer state.",
    "Add or update pipeline pager tests only if the public return shape or enqueue/ensure contract changes.",
    "Create the Coinduction launcher config artifact with Codex implementer, Codex reviewer, Codex pager route, bounded scope, and explicit constraints that it must be launched through launch_wave.py after this cleanup wave; do not implement Coinduction here.",
    "Collect the L4 indicator artifact and run the configured evidence command.",
    "Let launch_wave.py, Phase A, Phase B, bridge review, and the 15-step commit executor handle receipts, commit, PR, CI, merge, and post-merge cleanup."
  ],
  "constraints": [
    "Use launch_wave.py, executor_dispatch, Phase A, Phase B, bridge review, and commit executor; do not hand-author receipts, handoffs, commits, PRs, or merges.",
    "Implementer, reviewer, and pager route are Codex. The executor config supplies Codex 5.5 xhigh defaults.",
    "This wave is a control-plane cleanup and next-config preparation only.",
    "Do not implement Coinduction, Fixpoint, Optimization, runtime semantics, seed semantics, substrate semantics, registry semantics, projection semantics, evaluator semantics, parser semantics, or JavaScript parity semantics in this wave.",
    "Do not delete queued pager events as part of the implementation; stale queue cleanup already happened outside the wave and delivery logs remain evidence.",
    "Do not suppress pager delivery attempts globally. Bound duplicate drainer spawning while preserving eventual delivery attempts.",
    "Do not require valid Claude credentials for Codex-routed pipeline success.",
    "Do not change tmux layout, autoping target, model defaults, global executor config, or Codex cache surfaces unless Phase A proves the pager root fix cannot work without a narrower related edit."
  ],
  "stop_conditions": [
    "Halt as DEFECT if the fix can drop queued pager events, mark failed events as delivered, or prevent a future healthy credential state from draining the queue.",
    "Halt as DEFECT if duplicate ensure_draining calls can still spawn unbounded detached --once receiver processes for the same repo/bus while a matching live drainer exists.",
    "Halt as DEFECT if the implementation relies only on delivery.lock and not on a spawn/active-drainer guard, because delivery.lock alone already failed to prevent the process herd.",
    "Halt as DEFECT if the public pager path starts failing pipeline execution when Claude credentials are invalid.",
    "Halt as POLICY_BOUND if fixing the pager herd requires manual commit, manual PR, manual merge, or broad executor bypass.",
    "Halt as NEEDS_RESCOPING if this cleanup requires production runtime, substrate, seed, projection, parity, or math-structure semantic changes.",
    "Do not commit without focused tests, docs consistency, L4 contract enforcement, indicator collection, bridge review, and commit-executor handling."
  ],
  "acceptance_criteria": [
    "ensure_draining starts one detached --once receiver when no matching live drainer exists.",
    "A second ensure_draining call for the same repo/bus while that drainer is live does not call Popen again and reports the existing drainer as an accepted non-error state.",
    "Dead, stale, or command-mismatched drainer state is replaced by a fresh detached --once receiver.",
    "Queued event files remain durable and delivery serialization remains intact.",
    "pipeline_agent_pager _dispatch_claude remains fail-open for enqueue/drain scheduling and does not require healthy Claude credentials for Codex pipeline progress.",
    "Focused claude_pager_receiver and pipeline_agent_pager tests pass.",
    "A Coinduction launcher config artifact exists and is bounded to the next PROGRAM QUEUE item without implementing it in this cleanup wave.",
    "Configured evidence command and L4 indicator collection pass.",
    "Commit, PR, review, CI, merge, post-merge cleanup, and receipts are handled by commit executor, not manually."
  ],
  "request_for_agent": "Run this cleanup through the pipeline only. Fix claude_pager_receiver ensure_draining so pager failures cannot spawn another herd of detached --once receiver processes, update focused tests, preserve event durability and fail-open pager semantics, and create only the next Coinduction launcher config for subsequent pipeline execution. Keep implementation, review, and pager on Codex 5.5 xhigh. Do not manually author receipts, handoffs, commits, PRs, or merges, and do not implement Coinduction, Fixpoint, or Optimization in this cleanup wave.",
  "request_for_claude": "Compatibility input only: this wave is pinned to Codex implementer, Codex reviewer, and Codex pager route. Do not route implementation, review, pager handling, commit handling, or receipts to Claude.",
  "authorization_note": "Founder-directed autonomous cleanup after stale pager receiver herd and before continuing the remaining PROGRAM QUEUE. The founder explicitly requires pipeline use, commit-executor ownership of the 15-step commit path, Codex implementer/reviewer, and no manual receipts, handoffs, commits, PRs, or merges unless a real stop condition triggers.",
  "routing_decision": "ROUTE_PHASE_A",
  "routing_summary": "Control-plane pager-drain singleflight cleanup plus next Coinduction launcher-config preparation with Codex implementer, reviewer, pager route, and commit-executor ownership.",
  "tracked_packet": "reports/control_plane/claude-pager-drain-singleflight-2026-06-27_2026-06-27.md"
}
