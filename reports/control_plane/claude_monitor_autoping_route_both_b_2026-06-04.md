# Claude Monitor Autoping Route Both B 2026-06-04

Date: 2026-06-04
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: claude-monitor-autoping-route-both-2026-06-04b
Phase-A-Lock: LOCKED
Purpose: Complete #22 (Enable Claude pager route=both) by adding a codex-mirrored AUTOPING that keeps a DEDICATED Claude monitor warm, then flipping the pager route to 'both'. Wave A (PR #1074, in dev) already landed the foundation in mu/tools/observability/pipeline_agent_pager.py: CLAUDE_MONITOR_SESSION_ID_PATH (=.agent_bus/observability/claude_monitor_session_id), _read_claude_monitor_session_id (fail-closed, NO orchestrator fallback), the dedicated-resume _dispatch_claude (skip MONITOR_UNSET when absent, skip MONITOR_EQUALS_LIVE when the monitor id equals the live orchestrator id, else `claude --resume <monitor-id> -p`), the RETRYABLE monitor-absent skip in _dispatch_pending_locked, and the session-start.sh RCX_CLAUDE_MONITOR=1 writer. Route is still 'codex'. READ FIRST: mu/tools/session/codex_autoping_watch.py (the watcher to mirror -- but most of it is codex-app-server-specific: bridge.db polling, tmux integration, thread/turn protocol, fresh-exec context recovery -- OMIT all of those) and mu/tools/session/ensure_codex_autoping.sh (launcher to mirror, simplified); in pipeline_agent_pager.py the _read_claude_monitor_session_id / _read_orchestrator_session_id / _requested_targets / _dispatch_claude / _dispatch_pending_locked functions + the CLAUDE_MONITOR_SESSION_ID_PATH and CLAUDE_SKIP_REASON_* constants. DELIVER: (1) NEW mu/tools/session/claude_autoping_watch.py -- the GENERIC autoping core mirrored for the DEDICATED Claude monitor: resolve the dedicated monitor session id from .agent_bus/observability/claude_monitor_session_id (reuse the SAME resolution + fail-closed discipline as pipeline_agent_pager._read_claude_monitor_session_id; NEVER read or resume orchestrator_session_id -- pinging the live orchestrator is the interference Wave A exists to prevent); loop (--interval default 20.0s, after --initial-delay default 30.0s) keeping the monitor warm via a keepalive subprocess; write a pausable repo-matched state file .agent_bus/observability/claude_autoping_<slug>.json (version:1 + the generic codex fields: updated_at/watcher_pid/session_id/status/active_pid/active_log/last_exit_code/last_completed_at/last_summary/repo_root/bus_dir/summary_path/pause_reason), merge-update each tick; extract the one-line `Autoping summary:` from the keepalive output; on timeout SIGTERM the process group, 5s grace, then SIGKILL (mirror codex _terminate_process_group). PAUSE (do NOT crash, do NOT fall back to the orchestrator) when the monitor id is absent/malformed. OMIT codex specifics (bridge.db, tmux, thread/turn, fresh-exec). CLI args: --repo-root, --session-id (default: read claude_monitor_session_id), --interval, --initial-delay, --ping-timeout (default 120.0), --bus-dir. GAP-1 RESOLVED -- MECHANICAL tool-disable (NOT prompt-bounded): the keepalive `claude --resume <monitor-id> -p <prompt>` invocation MUST mechanically forbid the monitor from launching pipeline executors by passing claude's tool-restriction flag (e.g. `--disallowedTools` covering at minimum Bash -- the tool that could spawn executor_dispatch.py/phase_a/phase_b/commit_executor/bridge_supervisor -- or an equivalent restricted permission mode). The prompt instruction is a BACKUP, not the primary guard. A regression test asserts the keepalive argv contains the tool-restriction flag. GAP-2 RESOLVED -- STREAM-JSON: the keepalive command MUST pass `--output-format stream-json` (with `--verbose` if claude requires it for stream-json on -p) so the watcher can parse the terminal agent_message text for the `Autoping summary:` line. A test asserts the keepalive argv contains `--output-format stream-json` and that the summary parser extracts the line from a stream-json sample. (2) NEW mu/tools/session/ensure_claude_autoping.sh -- mirror ensure_codex_autoping.sh, SIMPLIFIED (no tmux): parse args (--repo, --session-id, --interval, --initial-delay, --ping-timeout, --bus-dir, --force-restart); env guards (skip if RCX_PIPELINE_SESSION=1, or if no monitor id is available); idempotency (read the state file's watcher_pid; if alive AND a valid claude_autoping_watch for this session, exit 0 unless --force-restart; else stop old + start new); orphan cleanup (ps-scan stray claude_autoping_watch.py ppid=1, SIGTERM); launch via nohup. GAP-3 RESOLVED -- ACTIVATION/HEALTH path for the route flip: ensure_claude_autoping.sh is the ACTIVATION entry point (the founder runs a dedicated monitor via RCX_CLAUDE_MONITOR=1 claude -> session-start.sh writes claude_monitor_session_id -> ensure_claude_autoping.sh starts the watcher that keeps it warm). The route=both flip is SAFE because Wave A's monitor-absent skip is RETRYABLE (a page before the monitor is up is re-queued, NOT dropped) -- so route=both never silently drops a page even if the monitor is not yet running. Document this activation+safety story in the packet/plan and add a HEALTH signal: the claude_autoping state file's `status` field reflects healthy/paused so an operator (and a future gate) can read monitor health. Do NOT gate the flip on the monitor being up (the retryable skip is the safety net); just ensure the activation path + health visibility exist. (3) FLIP mu/tools/executors/executor_config.json pipeline_agent_pager.route from "codex" to "both". (4) GROWTH CAPS: bump CAP_TOOL_SCRIPTS +2 (claude_autoping_watch.py + ensure_claude_autoping.sh) in mu/tests/docs/test_growth_caps.py with inline FOUNDER_OVERRIDE:claude-monitor-autoping-route-both-2026-06-04b comments; if a NEW test file is added, ALSO bump CAP_TEST_FILES +1 with the same comment. GAP-4 RESOLVED -- BOUND tests + evidence: the regression tests (name them concretely) MUST cover: (a) the keepalive targets claude_monitor_session_id NOT the orchestrator; (b) the keepalive argv contains the mechanical tool-restriction flag (GAP-1) AND --output-format stream-json (GAP-2); (c) absent/malformed monitor id -> paused, no orchestrator fallback; (d) the state file schema + status health field; (e) _requested_targets('both') -> ['codex','claude'] and a page dispatches to both (in the EXISTING mu/tests/tools/test_pipeline_agent_pager.py). The wave's evidence_command is exactly: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_claude_autoping_watch.py -k "route or both or claude or autoping"` (create test_claude_autoping_watch.py for the watcher; bump CAP_TEST_FILES +1 for it). SCOPE: claude_autoping_watch.py (new) + ensure_claude_autoping.sh (new) + executor_config.json (route flip) + test_growth_caps.py (cap bumps) + tests. The codex autoping subsystem and the codex pager leg MUST be ENTIRELY UNAFFECTED. L4_ENABLER: tooling/observability only, no runtime/substrate dir. Cite code by FUNCTION NAME only; NO file:line in the plan.

## Scope

#22 Wave B re-scope (attempt-1 diverged on 4 under-specified gaps; this packet resolves all 4 up front). L4_ENABLER: mirror the codex autoping GENERIC core for a DEDICATED Claude monitor + flip route=both. NEW claude_autoping_watch.py keeps claude_monitor_session_id (NEVER the orchestrator) warm via `claude --resume <id> -p` with (GAP-1) a MECHANICAL tool-restriction flag (--disallowedTools Bash/equiv, not just prompt) and (GAP-2) `--output-format stream-json` for summary parsing; writes a pausable .agent_bus/observability/claude_autoping_<slug>.json state (version:1 + health status); pauses (no orchestrator fallback) on absent id; omits codex specifics (bridge.db/tmux/thread-turn/fresh-exec). NEW ensure_claude_autoping.sh (no-tmux launcher) is the (GAP-3) ACTIVATION entry point; route=both is safe via Wave A's RETRYABLE monitor-absent skip (no silent drop) + the state status as a health signal (flip not gated on monitor-up). Flip executor_config route codex->both. Bump CAP_TOOL_SCRIPTS +2 (+CAP_TEST_FILES +1 for the new watcher test). (GAP-4) BOUND tests: keepalive targets dedicated monitor + contains the tool-restriction flag + stream-json; absent-id->paused; route=both->both targets. Codex autoping + codex leg ENTIRELY unaffected. Validation gate: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_claude_autoping_watch.py -k "route or both or claude or autoping"`. Cite by function name; no file:line.

- `reports/deferred/non_blocking/claude-monitor-autoping-route-both-2026-06-04b_bridge_nonblockers.md`
  - Same-wave Phase B/commit generated deferred non-blocking bridge findings packet only; no unrelated deferred report is authorized by this wave.

## Work items

Wave A (PR #1074, in dev) already landed the foundation in `pipeline_agent_pager.py` (`CLAUDE_MONITOR_SESSION_ID_PATH`, `_read_claude_monitor_session_id`, the dedicated-resume `_dispatch_claude`, the retryable monitor-absent skip in `_dispatch_pending_locked`) and the `session-start.sh` `RCX_CLAUDE_MONITOR=1` writer. Those are DONE per the packet and are NOT re-listed as pending here. Wave B (this packet) delivers, bounded:

1. NEW `mu/tools/session/claude_autoping_watch.py` — the generic autoping core mirrored for the DEDICATED Claude monitor. Resolve the monitor session id from `.agent_bus/observability/claude_monitor_session_id` reusing the same fail-closed discipline as `_read_claude_monitor_session_id`; NEVER read or resume `orchestrator_session_id`. Loop (`--interval` default 20.0s, after `--initial-delay` default 30.0s) keeping the monitor warm via a keepalive subprocess; write a pausable repo-matched state file `.agent_bus/observability/claude_autoping_<slug>.json` (version 1 + the generic codex fields `updated_at`/`watcher_pid`/`session_id`/`status`/`active_pid`/`active_log`/`last_exit_code`/`last_completed_at`/`last_summary`/`repo_root`/`bus_dir`/`summary_path`/`pause_reason`), merge-updated each tick; extract the one-line `Autoping summary:` from keepalive output; on timeout SIGTERM the process group, 5s grace, then SIGKILL (mirror codex `_terminate_process_group`). PAUSE (do not crash, do not fall back to the orchestrator) on absent/malformed monitor id. CLI: `--repo-root`, `--session-id` (default: read `claude_monitor_session_id`), `--interval`, `--initial-delay`, `--ping-timeout` (default 120.0), `--bus-dir`.
   - GAP-1 (mechanical tool-disable): the keepalive `claude --resume <monitor-id> -p <prompt>` argv MUST carry a mechanical tool-restriction flag (e.g. `--disallowedTools` covering at minimum Bash — the tool that could spawn `executor_dispatch.py`/`phase_a`/`phase_b`/`commit_executor`/`bridge_supervisor` — or an equivalent restricted permission mode). The prompt instruction is a BACKUP, not the primary guard.
   - GAP-2 (stream-json): the keepalive argv MUST pass `--output-format stream-json` (with `--verbose` if claude requires it for stream-json on `-p`) so the watcher can parse the terminal agent_message text for the `Autoping summary:` line.
2. NEW `mu/tools/session/ensure_claude_autoping.sh` — mirror `ensure_codex_autoping.sh`, SIMPLIFIED (no tmux): arg parse (`--repo`, `--session-id`, `--interval`, `--initial-delay`, `--ping-timeout`, `--bus-dir`, `--force-restart`); env guards (skip if `RCX_PIPELINE_SESSION=1`, or if no monitor id is available); idempotency via the state-file `watcher_pid`; orphan cleanup (ps-scan stray `claude_autoping_watch.py` with ppid 1, SIGTERM); launch via nohup.
   - GAP-3 (activation/health): this script is the ACTIVATION entry point (founder runs `RCX_CLAUDE_MONITOR=1 claude` -> `session-start.sh` writes `claude_monitor_session_id` -> `ensure_claude_autoping.sh` starts the watcher). Health is visible via the state-file `status` field (healthy/paused). The route=both flip is NOT gated on monitor-up; Wave A's RETRYABLE monitor-absent skip is the safety net (a page before the monitor is up is re-queued, not dropped).
3. FLIP `mu/tools/executors/executor_config.json` `pipeline_agent_pager.route` from `"codex"` to `"both"`.
4. GROWTH CAPS — bump `CAP_TOOL_SCRIPTS` +2 (the two new scripts) and `CAP_TEST_FILES` +1 (the new watcher test) in `mu/tests/docs/test_growth_caps.py`, each with an inline `FOUNDER_OVERRIDE:claude-monitor-autoping-route-both-2026-06-04b` comment.
5. BOUND tests (GAP-4) — concrete regression tests covering: (a) the keepalive targets `claude_monitor_session_id`, NOT the orchestrator; (b) the keepalive argv contains the mechanical tool-restriction flag (GAP-1) AND `--output-format stream-json` (GAP-2); (c) absent/malformed monitor id -> paused, no orchestrator fallback; (d) the state-file schema + `status` health field; (e) `_requested_targets('both')` -> `['codex','claude']` and a page dispatches to both. New `mu/tests/tools/test_claude_autoping_watch.py` for the watcher; the `_requested_targets`/route assertions go in the EXISTING `mu/tests/tools/test_pipeline_agent_pager.py`.

## Constraints (not in scope)

- The codex autoping subsystem (`codex_autoping_watch.py`, `ensure_codex_autoping.sh`) and the codex pager leg MUST be ENTIRELY UNAFFECTED.
- OMIT all codex-app-server specifics from the mirror: bridge.db polling, tmux integration, thread/turn protocol, fresh-exec context recovery.
- NEVER read or resume `orchestrator_session_id` — pinging the live orchestrator is the interference Wave A exists to prevent.
- L4_ENABLER: tooling/observability only — touch NO runtime/substrate dirs (no `mu/host/...`, no `rcx_pi/selfhost/...`).
- Do NOT gate the route=both flip on the monitor being up; the retryable monitor-absent skip is the safety net.
- Do NOT re-implement or re-touch the Wave A foundation already landed in `pipeline_agent_pager.py` / `session-start.sh`.
- Cite code by FUNCTION NAME; no file:line in the plan.
- This is Phase A (design). Do NOT implement in this turn.

## Stop conditions

- STOP once the packet contains the required Phase A sections (Scope, Work items, Constraints, Stop conditions, Acceptance criteria, Grounding / Authorization). Do NOT begin implementation in Phase A.
- Hard scope ceiling: exactly the SCOPE files — `claude_autoping_watch.py` (new), `ensure_claude_autoping.sh` (new), `executor_config.json` (route flip), `test_growth_caps.py` (cap bumps), `test_claude_autoping_watch.py` (new) + the existing `test_pipeline_agent_pager.py`. Touching anything else is a STOP-and-re-scope trigger.
- STOP and escalate to the founder if safe route=both would require gating the flip on monitor health, adding an orchestrator fallback, or otherwise contradicting Wave A's fail-closed / retryable design.
- STOP and escalate if the mechanical tool-restriction (GAP-1) cannot be expressed as a `claude` CLI argv flag — a prompt-only guard is NOT acceptable closure.
- STOP and re-scope if any deliverable would require touching the codex autoping subsystem, the codex pager leg, or any runtime/substrate dir.
- Do NOT use `--no-verify` or bypass any gate; commit only through the pipeline (`commit_executor.py`).

## Acceptance criteria

- The wave evidence command passes exactly: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_claude_autoping_watch.py -k "route or both or claude or autoping"`.
- The five GAP-4 assertions (a)–(e) above are present and green.
- `executor_config.json` `pipeline_agent_pager.route` reads `"both"`.
- `CAP_TOOL_SCRIPTS` +2 and `CAP_TEST_FILES` +1 in `test_growth_caps.py`, each annotated with the wave's `FOUNDER_OVERRIDE` comment; growth-cap tests green.
- The codex autoping subsystem and the codex pager leg are unchanged (no diff outside SCOPE).
- No runtime/substrate dir is touched (L4_ENABLER purity preserved).
- Indicator artifact collected: `python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id claude-monitor-autoping-route-both-2026-06-04b --output reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`.

## Grounding / Authorization

- TASKS.md authorization: this wave is authorized by the tracker sync note in `TASKS.md` dated 2026-06-04 for wave `claude-monitor-autoping-route-both-2026-06-04b` (queue label `[NEXT-CODEX-POST-REDTEAM]`, under the `## Ra` ledger). It records Class `L4_ENABLER`; `target_gate_id: G8`; Packet `reports/control_plane/claude_monitor_autoping_route_both_b_2026-06-04.md` (this file is the governing packet); `primary_blocker_class: INTEGRATION`; `primary_invariant_id: INV_TYPED_FAIL_CLOSED_OUTCOMES`; `bootstrap_endgame_policy: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP`; `boot0_track_id: V1`; `boot0_progress_state: HOLD`.
- Override (mechanical, same-wave): `FOUNDER_OVERRIDE:claude-monitor-autoping-route-both-2026-06-04b`
- Authorization: standing pipeline-bug-fix authorization per memory `feedback_autonomous_executor_fix.md` (control-surface / observability L4_ENABLER). The wave-bound `FOUNDER_OVERRIDE` token above lets commit automation derive the same-wave override mechanically for commit-gate + pre-push adjacency-cap clearance.
- Evidence (from the authorizing note): evidence_command `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_claude_autoping_watch.py -k "route or both or claude or autoping"`; indicator_artifact_ref `reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`; indicator_collection_command `python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id claude-monitor-autoping-route-both-2026-06-04b --output reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`.
- Governing packet / prior art: Wave A foundation = PR #1074 (`pipeline_agent_pager.py` dedicated-monitor reader/dispatch/writer + retryable skip). Mirror source = `codex_autoping_watch.py` / `ensure_codex_autoping.sh` (generic core only).

## Request from Post-Merge Supervisor

Complete #22 (Enable Claude pager route=both) by adding a codex-mirrored AUTOPING that keeps a DEDICATED Claude monitor warm, then flipping the pager route to 'both'. Wave A (PR #1074, in dev) already landed the foundation in mu/tools/observability/pipeline_agent_pager.py: CLAUDE_MONITOR_SESSION_ID_PATH (=.agent_bus/observability/claude_monitor_session_id), _read_claude_monitor_session_id (fail-closed, NO orchestrator fallback), the dedicated-resume _dispatch_claude (skip MONITOR_UNSET when absent, skip MONITOR_EQUALS_LIVE when the monitor id equals the live orchestrator id, else `claude --resume <monitor-id> -p`), the RETRYABLE monitor-absent skip in _dispatch_pending_locked, and the session-start.sh RCX_CLAUDE_MONITOR=1 writer. Route is still 'codex'. READ FIRST: mu/tools/session/codex_autoping_watch.py (the watcher to mirror -- but most of it is codex-app-server-specific: bridge.db polling, tmux integration, thread/turn protocol, fresh-exec context recovery -- OMIT all of those) and mu/tools/session/ensure_codex_autoping.sh (launcher to mirror, simplified); in pipeline_agent_pager.py the _read_claude_monitor_session_id / _read_orchestrator_session_id / _requested_targets / _dispatch_claude / _dispatch_pending_locked functions + the CLAUDE_MONITOR_SESSION_ID_PATH and CLAUDE_SKIP_REASON_* constants. DELIVER: (1) NEW mu/tools/session/claude_autoping_watch.py -- the GENERIC autoping core mirrored for the DEDICATED Claude monitor: resolve the dedicated monitor session id from .agent_bus/observability/claude_monitor_session_id (reuse the SAME resolution + fail-closed discipline as pipeline_agent_pager._read_claude_monitor_session_id; NEVER read or resume orchestrator_session_id -- pinging the live orchestrator is the interference Wave A exists to prevent); loop (--interval default 20.0s, after --initial-delay default 30.0s) keeping the monitor warm via a keepalive subprocess; write a pausable repo-matched state file .agent_bus/observability/claude_autoping_<slug>.json (version:1 + the generic codex fields: updated_at/watcher_pid/session_id/status/active_pid/active_log/last_exit_code/last_completed_at/last_summary/repo_root/bus_dir/summary_path/pause_reason), merge-update each tick; extract the one-line `Autoping summary:` from the keepalive output; on timeout SIGTERM the process group, 5s grace, then SIGKILL (mirror codex _terminate_process_group). PAUSE (do NOT crash, do NOT fall back to the orchestrator) when the monitor id is absent/malformed. OMIT codex specifics (bridge.db, tmux, thread/turn, fresh-exec). CLI args: --repo-root, --session-id (default: read claude_monitor_session_id), --interval, --initial-delay, --ping-timeout (default 120.0), --bus-dir. GAP-1 RESOLVED -- MECHANICAL tool-disable (NOT prompt-bounded): the keepalive `claude --resume <monitor-id> -p <prompt>` invocation MUST mechanically forbid the monitor from launching pipeline executors by passing claude's tool-restriction flag (e.g. `--disallowedTools` covering at minimum Bash -- the tool that could spawn executor_dispatch.py/phase_a/phase_b/commit_executor/bridge_supervisor -- or an equivalent restricted permission mode). The prompt instruction is a BACKUP, not the primary guard. A regression test asserts the keepalive argv contains the tool-restriction flag. GAP-2 RESOLVED -- STREAM-JSON: the keepalive command MUST pass `--output-format stream-json` (with `--verbose` if claude requires it for stream-json on -p) so the watcher can parse the terminal agent_message text for the `Autoping summary:` line. A test asserts the keepalive argv contains `--output-format stream-json` and that the summary parser extracts the line from a stream-json sample. (2) NEW mu/tools/session/ensure_claude_autoping.sh -- mirror ensure_codex_autoping.sh, SIMPLIFIED (no tmux): parse args (--repo, --session-id, --interval, --initial-delay, --ping-timeout, --bus-dir, --force-restart); env guards (skip if RCX_PIPELINE_SESSION=1, or if no monitor id is available); idempotency (read the state file's watcher_pid; if alive AND a valid claude_autoping_watch for this session, exit 0 unless --force-restart; else stop old + start new); orphan cleanup (ps-scan stray claude_autoping_watch.py ppid=1, SIGTERM); launch via nohup. GAP-3 RESOLVED -- ACTIVATION/HEALTH path for the route flip: ensure_claude_autoping.sh is the ACTIVATION entry point (the founder runs a dedicated monitor via RCX_CLAUDE_MONITOR=1 claude -> session-start.sh writes claude_monitor_session_id -> ensure_claude_autoping.sh starts the watcher that keeps it warm). The route=both flip is SAFE because Wave A's monitor-absent skip is RETRYABLE (a page before the monitor is up is re-queued, NOT dropped) -- so route=both never silently drops a page even if the monitor is not yet running. Document this activation+safety story in the packet/plan and add a HEALTH signal: the claude_autoping state file's `status` field reflects healthy/paused so an operator (and a future gate) can read monitor health. Do NOT gate the flip on the monitor being up (the retryable skip is the safety net); just ensure the activation path + health visibility exist. (3) FLIP mu/tools/executors/executor_config.json pipeline_agent_pager.route from "codex" to "both". (4) GROWTH CAPS: bump CAP_TOOL_SCRIPTS +2 (claude_autoping_watch.py + ensure_claude_autoping.sh) in mu/tests/docs/test_growth_caps.py with inline FOUNDER_OVERRIDE:claude-monitor-autoping-route-both-2026-06-04b comments; if a NEW test file is added, ALSO bump CAP_TEST_FILES +1 with the same comment. GAP-4 RESOLVED -- BOUND tests + evidence: the regression tests (name them concretely) MUST cover: (a) the keepalive targets claude_monitor_session_id NOT the orchestrator; (b) the keepalive argv contains the mechanical tool-restriction flag (GAP-1) AND --output-format stream-json (GAP-2); (c) absent/malformed monitor id -> paused, no orchestrator fallback; (d) the state file schema + status health field; (e) _requested_targets('both') -> ['codex','claude'] and a page dispatches to both (in the EXISTING mu/tests/tools/test_pipeline_agent_pager.py). The wave's evidence_command is exactly: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_claude_autoping_watch.py -k "route or both or claude or autoping"` (create test_claude_autoping_watch.py for the watcher; bump CAP_TEST_FILES +1 for it). SCOPE: claude_autoping_watch.py (new) + ensure_claude_autoping.sh (new) + executor_config.json (route flip) + test_growth_caps.py (cap bumps) + tests. The codex autoping subsystem and the codex pager leg MUST be ENTIRELY UNAFFECTED. L4_ENABLER: tooling/observability only, no runtime/substrate dir. Cite code by FUNCTION NAME only; NO file:line in the plan.

<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:start -->
## Same-Wave Deferred Non-Blocking Authorization

- Refresh wave: `claude-monitor-autoping-route-both-2026-06-04b`
- Purpose: Phase B and commit automation may stage the same-wave non-blocking bridge findings packet as deferred follow-up instead of blocking an otherwise commit-ready wave.
- Authorized deferred packet(s):
  - `reports/deferred/non_blocking/claude-monitor-autoping-route-both-2026-06-04b_bridge_nonblockers.md`
- Scope binding: the packet(s) above are in scope only as generated same-wave non-blocking bridge findings packets.
- Acceptance binding: the final touched-file set may include the packet(s) above when they are also present in `deferred_items` or current staged files.
<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `claude-monitor-autoping-route-both-2026-06-04b`
- Active packet: `reports/control_plane/claude_monitor_autoping_route_both_b_2026-06-04.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `0cdd93f5bda83da49d1c375367326446b7c8434ac5a3ccd5dfc6b2eb688d215e`
- Indicator artifact: `reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/tools/test_claude_autoping_watch.py mu/tests/tools/test_pipeline_agent_pager.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/claude_monitor_autoping_route_both_b_2026-06-04.md. (2) Final pytest gate covered 3 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package.
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/docs/test_growth_caps.py`
  - `mu/tests/tools/test_claude_autoping_watch.py`
  - `mu/tests/tools/test_pipeline_agent_pager.py`
  - `mu/tools/executors/executor_config.json`
  - `mu/tools/session/claude_autoping_watch.py`
  - `mu/tools/session/ensure_claude_autoping.sh`
  - `reports/control_plane/claude_monitor_autoping_route_both_b_2026-06-04.md`
  - `reports/deferred/non_blocking/claude-monitor-autoping-route-both-2026-06-04b_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/claude-monitor-autoping-route-both-2026-06-04b.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
