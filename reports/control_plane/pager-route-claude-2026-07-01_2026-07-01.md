# Pager route to Claude to match all-Claude roles (no codex)

Date: 2026-07-01
Status: Phase B (pre-supervisor pending, bridge-converged)
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: pager-route-claude-2026-07-01
Phase-A-Lock: LOCKED
Purpose: FOUNDER DIRECTIVE (2026-07-01, non-interactive autonomous session): the pipeline must run Claude everywhere with codex in NO active role, INCLUDING the pager. This wave is based on the roles-claude-opus branch (role_agents already flipped to claude/claude), and completes the all-Claude config by fixing the one remaining codex reference the Codex bot flagged as P1: pipeline_agent_pager.route is still 'codex' in the committed executor_config.json AND in the in-code DEFAULT_EXECUTOR_CONFIG, so a clean checkout with no bus-local orchestrator override still pages Codex (the pager _resolve_route falls back to that value and _requested_targets('codex') sends lifecycle events to Codex). Fix: set pipeline_agent_pager.route to 'claude' in BOTH executor_config.json and the in-code DEFAULT_EXECUTOR_CONFIG, so clean-checkout pipeline waves page Claude by default, consistent with the all-Claude roles. Then update every test that asserts the pager route is 'codex' (for example the set_roles config test, and the recovery-gate and orchestrator-mode pager-route fixtures/assertions) to expect 'claude' where the default now applies; keep tests that deliberately seed an explicit codex route (to exercise codex-route behavior) intact and route-agnostic. LOSSLESS: change ONLY the default route value + the tests that assert the default; do NOT weaken or delete any assertion, do NOT change pager behavior/logic beyond the default value, do NOT touch runtime substrate dirs, do NOT add a fallback-model. Verify no active codex pager route remains for the default/all-claude path. This resolves the PR #1196 bot P1 and lands the all-Claude config (roles + pager) together.
GATE AUTHORITY (fixes the bridge REQUEST_CHANGES): the evidence gate that the #52 pre-commit supervisor and the bot actually EXECUTE is the TASKS.md tracker sync note's `evidence_command`, not this packet's copy. The supervisor package reads it via `_tracker_evidence_command_value(pre_supervisor_tracker_note)` (`mu/tools/executors/phase_b_executor.py` and `:9148`), and `reconcile_packet_l4_fields_block_for_wave` makes the NOTE win over the packet for `evidence_command` (it is a note-sourced field in `_L4_FIELDS_FROM_TRACKER`, `mu/tools/executors/commit_executor.py`; reconcile at `:2031`). Therefore the strengthened gate MUST be carried by the NOTE. The prior packet strengthened only line-43 of this file while the wave note (TASKS.md) kept the WEAK gate -- so the strong packet gate is a DEAD LETTER: the weak note gate is what runs, and note-wins reconcile even reverts this packet's strong `evidence_command` back to the weak note value. The weak note gate asserts ONLY the committed `executor_config.json` route and runs ONLY `test_set_roles.py` + `test_orchestrator_mode_switch.py` (both SEED their own pager route, so they pass regardless), so the half-flip R1 rejected (JSON flipped, in-code DEFAULT still codex; clean checkouts still page Codex) still false-greens. The authoritative fix is to re-sync the NOTE's `evidence_command` to the strong gate and keep this packet's `evidence_command` byte-identical to it so `reconcile_packet_l4_fields_block_for_wave` is a no-op.

## Scope

executor_config.json (pipeline_agent_pager.route -> claude), executor_common.py DEFAULT_EXECUTOR_CONFIG (pipeline_agent_pager.route -> claude), the pager-route tests/fixtures that assert the default route is codex (set_roles, recovery_gate, orchestrator_mode), and the TASKS.md tracker sync note's `evidence_command` for this wave (the authoritative gate surface the #52 supervisor executes). No runtime substrate, no pager logic change, no fallback-model. Roles flip is inherited from the base branch, unchanged here.

Files and surfaces in scope:

- `mu/tools/executors/executor_config.json` -- committed default `pipeline_agent_pager.route`.
- `mu/tools/executors/executor_common.py` -- in-code `DEFAULT_EXECUTOR_CONFIG["pipeline_agent_pager"]["route"]`.
- `mu/tests/tools/test_pipeline_agent_pager.py`, `mu/tests/tools/test_set_roles.py`, `mu/tests/tools/test_orchestrator_mode_switch.py` -- pager-route tests.
- TASKS.md -- tracker-sync authority. The 2026-07-01 tracker sync note for wave `pager-route-claude-2026-07-01` (TASKS.md) is the single source of truth for this packet's L4 fields; the packet derives from it. Its `evidence_command` is the actual gate the #52 pre-commit supervisor runs and the note-wins reconcile source, so re-syncing that field to the strong gate is the authoritative fix -- NOT a packet-only edit.

- `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`
  - Same-wave Phase B/commit generated deferred non-blocking bridge findings packet only; no unrelated deferred report is authorized by this wave.

## Work items

1. Set `pipeline_agent_pager.route` from `codex` to `claude` in the committed `mu/tools/executors/executor_config.json` (currently `codex` at line 45 -- the fallback a clean checkout reads).
2. Set `pipeline_agent_pager.route` from `codex` to `claude` in the in-code `DEFAULT_EXECUTOR_CONFIG` at `mu/tools/executors/executor_common.py`, so a clean checkout with no bus-local `orchestrator_mode.json` override no longer resolves the codex fallback (`_resolve_route` -> `_requested_targets('codex')`). Both surfaces MUST flip together: flipping only the JSON while the in-code DEFAULT stays `codex` is the exact half-flip false-green the bridge flagged.
3. Flip the single committed-default gate test `mu/tests/tools/test_pipeline_agent_pager.py::test_executor_config_default_pager_route_is_codex` (reads the committed `executor_config.json`; currently defined at line 2773) to expect `claude`: rename to `test_executor_config_default_pager_route_is_claude`, update the docstring, and assert `route == "claude"`. This is the ONLY test that actually reads the committed default route; it fails after Work item 1 until updated.
4. Update fixtures/assertions that mirror the committed default as `codex` where the default now applies -- `mu/tests/tools/test_set_roles.py` (config fixture at line 31 and the preserved-route assertion at line 114) -- to `claude`, keeping `set_roles` route-agnostic (it must not rewrite the pager route; it only preserves what the fixture seeds).
5. Leave every test that deliberately SEEDS an explicit `codex`/`both` route to exercise route/narrowing/precedence behavior intact and route-agnostic: `mu/tests/tools/test_orchestrator_mode_switch.py` (`_seed_repo` route param, `both`/`codex` narrowing cases) and the explicit-route contract tests in `test_pipeline_agent_pager.py` (env-override precedence, bus-local precedence, explicit-codex-narrows-both, notify-only). Do NOT weaken, delete, or make these depend on the default.
6. AUTHORITATIVE GATE FIX -- re-sync the TASKS.md tracker sync note for wave `pager-route-claude-2026-07-01` (currently TASKS.md) so its `evidence_command` is the strong gate byte-identical to the Validation gates command below (asserts the in-code `DEFAULT_EXECUTOR_CONFIG` route AND runs `test_pipeline_agent_pager.py`, not only the committed JSON + `test_set_roles.py`/`test_orchestrator_mode_switch.py`). Do this via the tracker-sync path (`tracker_sync_note.py` / the Phase B handoff / `reconcile_packet_l4_fields_block_for_wave`), NOT a hand-edit of TASKS.md and NOT a manual `git add`. Rationale: the #52 pre-commit supervisor RUNS the NOTE's `evidence_command` (`phase_b_executor.py`, `:9148`), and `evidence_command` is a note-sourced field (`commit_executor.py:_L4_FIELDS_FROM_TRACKER` line 1458) that the note-wins `reconcile_packet_l4_fields_block_for_wave` (`commit_executor.py`) reverts to the note's value -- so strengthening only this packet is a dead letter and the weak note gate false-greens the half-flip. Keep the note's `evidence_command` a canonical single-backtick codespan (the strong command contains no backticks) so `_tracker_evidence_command_value` extracts it fail-closed, per the `test_tracker_marker_codespan_extraction.py` contract.

## Constraints

- Not in scope: any runtime substrate dir (`mu/host/`, `rcx_pi/selfhost/`, engine/kernel). This is an L4_ENABLER control-plane change; touching runtime is out of class.
- No pager logic/behavior change beyond the single default `route` value: `_resolve_route`, `_requested_targets`, env-override precedence, and bus-local `orchestrator_mode.json` precedence are unchanged (they still win over the committed default).
- No fallback-model added.
- No change to `role_agents` -- the claude/claude roles flip is inherited from the base roles-claude-opus branch and is not re-touched here.
- Do NOT weaken, delete, or make route-agnostic any assertion that deliberately exercises an explicit `codex`/`both` route.
- Do NOT hand-edit the TASKS.md tracker note or manually `git add` it; the note is re-synced only through the tracker-sync path so note-wins reconcile stays deterministic.

## Stop conditions

- STOP if flipping the in-code `DEFAULT_EXECUTOR_CONFIG` route requires editing pager resolution logic or any runtime substrate dir -- re-scope, do not proceed.
- STOP if the committed `executor_config.json` and the in-code `DEFAULT_EXECUTOR_CONFIG` end up divergent (only one flipped) -- a half-flip re-creates the reviewer's false-green (clean checkouts still page Codex while green passes).
- STOP if the TASKS.md wave note's `evidence_command` and this packet's `evidence_command` diverge -- note-wins `reconcile_packet_l4_fields_block_for_wave` reverts this packet's strong gate to the note's value, so a strong packet with a weak note is the dead-letter the #52 supervisor false-greens on. Both must be the identical strong gate.
- STOP and report if any test that seeds an EXPLICIT `codex`/`both` route regresses from the default flip (i.e. it is not actually route-agnostic); do not silently flip its assertion.
- STOP if the change reaches beyond the two default `route` values, the tests that assert the committed default, and the wave note's `evidence_command`.

## Validation gates

- evidence_command (the strong gate -- must be identical in this packet AND in the TASKS.md wave note): `python3 -c "import json; from mu.tools.executors.executor_common import DEFAULT_EXECUTOR_CONFIG; c=json.load(open('mu/tools/executors/executor_config.json')); assert c['pipeline_agent_pager']['route']=='claude', 'committed executor_config.json route still '+c['pipeline_agent_pager']['route']; assert DEFAULT_EXECUTOR_CONFIG['pipeline_agent_pager']['route']=='claude', 'in-code DEFAULT_EXECUTOR_CONFIG route still '+DEFAULT_EXECUTOR_CONFIG['pipeline_agent_pager']['route']; print('pager.route=claude in committed config AND in-code DEFAULT OK')" && python3 -m pytest mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_set_roles.py mu/tests/tools/test_orchestrator_mode_switch.py -q`
- Why this gate, and why it must live in the NOTE (fixes the bridge REQUEST_CHANGES): the R1 gate -- and the gate still sitting in the wave note at TASKS.md -- asserts ONLY the committed `executor_config.json` route and runs ONLY `test_set_roles.py`/`test_orchestrator_mode_switch.py`. Those tests SEED their own pager route (`test_set_roles.py` seeds `codex`; `test_orchestrator_mode_switch.py` `_seed_repo` route param defaults `both`), so they pass regardless of the in-code `DEFAULT_EXECUTOR_CONFIG` -- a half-flip (JSON flipped, in-code DEFAULT still `codex`) passes green while clean checkouts still page Codex. The strong gate above additionally (a) imports and asserts the in-code `DEFAULT_EXECUTOR_CONFIG` route directly, and (b) runs `test_pipeline_agent_pager.py`, whose `test_executor_config_default_pager_route_is_*` case is the only test that reads the committed default route. R1 rejected the half-flip; R2's earlier fix strengthened only this packet's line-43 copy, which is a DEAD LETTER because the #52 pre-commit supervisor executes the NOTE's `evidence_command` (`phase_b_executor.py`, `:9148`) and note-wins `reconcile_packet_l4_fields_block_for_wave` (`commit_executor.py`; `evidence_command` in `_L4_FIELDS_FROM_TRACKER` at `:1458`) reverts the packet copy to the note's weak value. The real fix (Work item 6) re-syncs the strong gate INTO the note and keeps this packet byte-identical, so the gate the supervisor actually runs is strong and catches the half-flip. Verified fail-closed on the current `codex` state: the strong command exits 1 with `AssertionError: committed executor_config.json route still codex`. Matches learning `reference_pager_route_default_flip_evidence` ("Re-sync TASKS.md note or note-wins reverts").

## Acceptance criteria

- `mu/tools/executors/executor_config.json` `pipeline_agent_pager.route == "claude"`.
- In-code `DEFAULT_EXECUTOR_CONFIG["pipeline_agent_pager"]["route"] == "claude"` (`mu/tools/executors/executor_common.py`).
- `mu/tests/tools/test_pipeline_agent_pager.py` committed-default test asserts `route == "claude"` (renamed to `test_executor_config_default_pager_route_is_claude`) and the full file is green -- explicit-route contract tests (env-override, bus-local, explicit-codex-narrows-both, notify-only) pass unchanged.
- `test_set_roles.py` and `test_orchestrator_mode_switch.py` green; no explicit-`codex`/`both`-route assertion weakened or deleted.
- The TASKS.md wave note (`pager-route-claude-2026-07-01`, TASKS.md) `evidence_command` is the strong gate -- it asserts the in-code `DEFAULT_EXECUTOR_CONFIG` route AND runs `mu/tests/tools/test_pipeline_agent_pager.py`, and is byte-identical to this packet's Validation-gates `evidence_command`, so `reconcile_packet_l4_fields_block_for_wave` is a no-op and the #52 supervisor executes the strong gate (not the weak note gate). Confirm the packet↔note match with `reconcile_packet_l4_fields_block_for_wave` producing no change to this packet.
- No active codex default pager route remains: `grep -R '"route": "codex"' mu/tools/executors/executor_config.json mu/tools/executors/executor_common.py` returns nothing (each file has exactly one such line today -- the pager default at `executor_config.json` and `executor_common.py`).
- Roles unchanged (`role_agents` still claude/claude from the base branch); no fallback-model; no runtime substrate dir touched.
- evidence_command exits 0.

## Grounding / Authorization

- Task: [NEXT-CODEX-POST-REDTEAM]; wave id `pager-route-claude-2026-07-01`.
- Governing packet: this file, `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`.
- TASKS.md authority: the 2026-07-01 tracker sync note for wave `pager-route-claude-2026-07-01` (TASKS.md) is canonical for this packet's L4 fields; its `evidence_command` is the gate the #52 pre-commit supervisor executes and the note-wins reconcile source, so it must carry the strong gate (Work item 6).
- Authorization: Founder-authorized 2026-07-01: Claude does everything incl. pager; codex nowhere. Resolves PR #1196 bot P1. FOUNDER_OVERRIDE:pager-route-claude-2026-07-01.

FOUNDER_OVERRIDE:pager-route-claude-2026-07-01

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/pager-route-claude-2026-07-01.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id pager-route-claude-2026-07-01 --output reports/l4_wave_indicators/pager-route-claude-2026-07-01.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_set_roles.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor package is staged at .scratch/phase_b_supervisor_package.json; commit handoff receipt remains pending the supervisor decision. scope_refs: `TASKS.md`, `mu/tests/tools/test_pipeline_agent_pager.py`, `mu/tests/tools/test_set_roles.py`, `mu/tools/executors/executor_common.py`, `mu/tools/executors/executor_config.json`, `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`, `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`, `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`..
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: pager-route-claude-2026-07-01.
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:start -->
## Phase B Indicator Scope Reconciliation

- Refresh wave: `pager-route-claude-2026-07-01`
- Active packet: `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`
- Indicator artifact: `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`
- Purpose: Phase B mechanically collected and staged this same-wave L4 indicator before pre-commit supervisor review so the tracker note, Gate 8 package, and governing packet describe one staged scope.
- Scope binding: no indicator file other than the artifact above is in scope for this wave.
- Authorized staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_pipeline_agent_pager.py`
  - `mu/tests/tools/test_set_roles.py`
  - `mu/tools/executors/executor_common.py`
  - `mu/tools/executors/executor_config.json`
  - `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`
  - `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`
<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:end -->

<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:start -->
## Same-Wave Deferred Non-Blocking Authorization

- Refresh wave: `pager-route-claude-2026-07-01`
- Purpose: Phase B and commit automation may stage the same-wave non-blocking bridge findings packet as deferred follow-up instead of blocking an otherwise commit-ready wave.
- Authorized deferred packet(s):
  - `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`
- Scope binding: the packet(s) above are in scope only as generated same-wave non-blocking bridge findings packets.
- Acceptance binding: the final touched-file set may include the packet(s) above when they are also present in `deferred_items` or current staged files.
<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `pager-route-claude-2026-07-01`
- Active packet: `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `8357096415dce528921e42736feb8d407b946779d75ed523e5ec319273b66618`
- Indicator artifact: `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_pipeline_agent_pager.py mu/tests/tools/test_set_roles.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor package is staged at .scratch/phase_b_supervisor_package.json; commit handoff receipt remains pending the supervisor decision. scope_refs: `TASKS.md`, `mu/tests/tools/test_pipeline_agent_pager.py`, `mu/tests/tools/test_set_roles.py`, `mu/tools/executors/executor_common.py`, `mu/tools/executors/executor_config.json`, `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`, `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`, `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`..
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_pipeline_agent_pager.py`
  - `mu/tests/tools/test_set_roles.py`
  - `mu/tools/executors/executor_common.py`
  - `mu/tools/executors/executor_config.json`
  - `reports/control_plane/pager-route-claude-2026-07-01_2026-07-01.md`
  - `reports/deferred/non_blocking/pager-route-claude-2026-07-01_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/pager-route-claude-2026-07-01.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
