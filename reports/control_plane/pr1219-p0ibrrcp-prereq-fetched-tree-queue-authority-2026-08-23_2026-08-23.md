# PR 1219 P0IBRRCP Prerequisite Fetched Tree Queue Authority 2026-08-23

Date: 2026-08-23
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [ROLES-ALL-CODEX-PR1219-P0IBRRCP-PREREQ-FETCHED-TREE-QUEUE-AUTHORITY]
Wave ID: pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23
Phase-A-Lock: LOCKED
Purpose: Land only the active post-merge queue-source defect: preserve the existing dirty-linked-dev warning path, resolve the fetched origin/dev commit once, derive every queue/package input from immutable Git objects at that full SHA, and keep package output at the existing verify-root active bus without checkout, detach, ref, cleanup, continuation, dispatcher, or recovery changes.

## Scope

One active commit-executor defect only: separate immutable queue-content authority from the existing package-output root. On the already-existing dirty linked-base path, read TASKS, packet status and wave identity, matching WaveConfigs, blocker paths, and merge history from Git objects at the exact fetched merge SHA; continue writing post_merge_package.json under the existing verify-root active bus. Do not alter resolver, checkout, cleanup, continuation, dispatcher, supervisor, recovery, or queue semantics.

Files and surfaces in scope:

- mu/tools/executors/commit_executor.py (MODIFY) -- add a bounded optional exact-commit object view for queue inputs and thread it only through existing queue-selection helpers; activate it only after the dirty linked-base branch has resolved the fetched full SHA; preserve package output at verify_root and every existing default caller.
- mu/tests/tools/test_commit_executor_post_merge_cleanup.py (MODIFY) -- add focused real-Git exact-commit queue/package coverage over a deliberately stale dirty filesystem, including SHA-bounded merge-history completion and unchanged Git-visible dirty state; run the entire module as mandatory evidence.
- mu/tests/tools/test_executor_dispatch.py (MODIFY) -- update only the existing dirty linked-base Step-15 regression to prove the recorded merge SHA is passed as queue authority while the clean linked-base regression remains unchanged; add required ANTICHEAT_OK markers only where this wave's direct private-helper test access requires them.
- TASKS.md (PIPELINE TRACKER NOTE ONLY) -- record this exact fetched-tree prerequisite without changing PROGRAM QUEUE content, current/next identity, or any existing task obligation.
- reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md (GENERATED) -- single governing packet.
- reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json (GENERATED BY COMMIT STEP 5) -- candidate-bound indicator; expected to be absent during Phase A before code is staged.
- reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md (GENERATED ONLY IF NEEDED) -- nonblocking observations only.
- TASKS.md -- tracker-sync authority. The 2026-08-23 tracker sync note for wave `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23` is the single source of truth for this packet's L4 fields; the packet derives from it.

- `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`
  - Same-wave Phase B/commit generated deferred non-blocking bridge findings packet only; no unrelated deferred report is authorized by this wave.

## Work items

1. Keep the exact-base behavior that fetches origin/base, detects a dirty linked base, does not ff-merge that dirty tree, resolves the full origin/base SHA, records it as merge_sha, warns, clears the existing continuation record, and proceeds. Do not import any stopped R4 detach or cleanup implementation.
2. Add one bounded optional queue_commit_sha authority inside commit_executor.py. When nonempty, read required blobs with full-SHA-qualified Git object commands, enumerate matching control-plane configs and blocking reports with full-SHA-qualified tree commands, and query completion history with git log bounded to that same full SHA. Validate all repo-relative inputs and never consult a moving ref after SHA resolution.
3. Thread the optional authority only through the queue-reading chain so TASKS program/routed entries and tracker notes, WaveConfig discovery/content, tracked-packet existence/status/wave identity, blocker report enumeration, and merge-history completion all come from the same commit. Empty authority must preserve every existing filesystem-backed caller and behavior.
4. In the reproduced dirty linked-base branch only, set queue_commit_sha to the already-recorded merge SHA and pass it into post-merge package refresh. Package path, result fields, and active bus stay rooted at the existing verify_root exactly as today; the object authority is content input only and must never become a filesystem or lifecycle root.
5. Fail closed if exact object truth cannot be read: do not fall back to dirty linked-dev files, stale candidate files, moving refs, or a fabricated queue-empty package, and do not overwrite an existing package with partial/stale truth.
6. Prove with a real repository that legacy filesystem parsing over a stale dirty tree selects the wrong item while exact-commit parsing selects the expected successor from that commit, including config, packet, blocker, and SHA-bounded merge-history inputs. Preserve the dirty checkout's branch, resolved HEAD, refs/heads/dev, porcelain state, and tracked/untracked content apart from existing ignored active-bus output.
7. Do not modify the stopped Q2 or R4 candidates. After this prerequisite lands, rebuild Q2 fresh from its exact merge and restore the preserved canonical broad-P0IB requirement text before review.

## Constraints

- No resolver, cleanliness-probe, checkout, detach, merge, worktree selection/removal, branch/stash cleanup, continuation ordering, dispatcher, supervisor, recovery, package schema, or queue-semantic change. Existing Step-15 and Step-16 behavior remains byte-for-byte outside the minimal queue-authority plumbing.
- Do not create a filesystem snapshot, temporary checkout, detached worktree, private branch, durable lifecycle metadata, lease, lock, retirement protocol, repeated status/TOCTOU protocol, atomic-package protocol, or new persisted authority field.
- Git-object reads must be qualified by the recorded full commit SHA. No post-resolution read may use origin/dev, dev, HEAD, or another moving symbolic name as queue-content authority.
- TASKS PROGRAM QUEUE and every existing task obligation must remain byte-for-byte unchanged; only the pipeline-generated same-wave tracker note may change TASKS.md.
- Preserve the dirty linked dev worktree's branch, resolved HEAD, refs/heads/dev, exact porcelain state, and tracked/untracked content apart from the already-existing ignored active-bus package output. Index stat-cache refresh caused by the baseline status probe is not part of this packet and cannot hold landing.
- The stopped R4 candidate and its staged evidence remain immutable. Do not copy, amend, rebase, stage, commit, push, merge, or otherwise mutate it.
- The external root WaveConfig is launch input only and cannot enter candidate staging, inventory, commit, PR, or merge.
- Do not modify launch_wave.py, executor_dispatch.py production code, phase executors, bridge_supervisor.py, meta_bridge_supervisor.py, executor_common.py, runtime, substrate, hosts, seeds, registries, projections, pager, model roles, Claude-owned files, or unrelated docs.
- No manual candidate patching, git add, index mutation, commit, push, merge, or direct PR mutation; launch_wave.py, dispatcher, Phase A, Phase B, and providerless commit own candidate and Git actions.

## Stop conditions

- Halt before launch if origin/dev, immutable linked source HEAD, fresh target initial HEAD, and comparison_commit do not all equal 15356f3971ad3480b9d52271f2396a41c45541e7.
- Halt as NEEDS_RESCOPING only if immutable queue truth cannot be implemented inside commit_executor.py and the two declared test modules without changing queue semantics or another production surface.
- Halt as DEFECT if any dirty-path queue input still comes from the live filesystem or a moving ref; if object-read failure falls back or overwrites the package; if package merge_sha differs from the authority SHA; or if package output moves away from the existing verify-root bus.
- Halt as DEFECT if this wave adds checkout, detach, ref movement, worktree lifecycle, cleanup, continuation, dispatcher, supervisor, or recovery behavior, or mutates the valuable Git-visible state of the dirty dev worktree.
- Do not halt, widen, or request changes for index stat-cache refresh, repeated status revalidation, locks, leases, ignored-file inventory, cleanup exception hardening, continuation/package atomicity, long-term candidate lifecycle or retirement, telemetry/provenance polish, documentation edge cases, or any nonblocking/non-occurring finding outside the reproduced path. Record such findings only in the optional nonblocker report and continue landing.

## Validation gates

- evidence_command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_commit_executor_post_merge_cleanup.py mu/tests/tools/test_executor_dispatch.py`

## Acceptance criteria

- The dirty linked-base path resolves origin/base once to a full commit SHA and supplies that exact value as merge_sha and queue_commit_sha; symbolic refs are not reused for queue parsing.
- Every queue input -- TASKS text, tracker notes, matching WaveConfig content, packet existence/status/wave identity, blocker paths, and merge-history completion -- is read or enumerated from the same SHA-qualified Git object view.
- A deliberately stale dirty filesystem selects the wrong item through legacy filesystem parsing while the fetched-SHA authority selects the expected successor and uses only config, packet, blocker, and history truth from that commit.
- Package merge_sha equals the authority SHA and post_merge_package.json is written under the same existing verify-root active bus as exact-base behavior; no snapshot, checkout, detach, alternate artifact root, or lifecycle state is introduced.
- Dirty linked dev branch, resolved HEAD, refs/heads/dev, exact porcelain state, and tracked/untracked content remain unchanged apart from existing ignored active-bus output. Baseline status stat-cache behavior is explicitly outside scope.
- Clean linked-dev fetch/ff-only behavior, invalid linked-root fallback behavior, package schema, queue semantics, and Step-16 cleanup remain green and unchanged.
- Only TASKS tracker-note truth, commit_executor.py, the two focused test modules, generated packet/indicator, and optional same-wave nonblocker change. Independent review, staged L4, providerless commit, CI, and merge pass.

## Grounding / Authorization

- Task: [ROLES-ALL-CODEX-PR1219-P0IBRRCP-PREREQ-FETCHED-TREE-QUEUE-AUTHORITY]; wave id `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23`.
- Governing packet: this file, `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`.
- TASKS.md authority: the 2026-08-23 tracker sync note for wave `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23` is canonical for this packet's L4 fields.
- Authorization: Founder directed autonomous landing through launch_wave.py, preservation of valuable state, narrower packets when a wave diverges, and deferral of nonblocking or non-occurring edge cases until Mu production.

FOUNDER_OVERRIDE:pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23

<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:start -->
## Phase B Indicator Scope Reconciliation

<!-- PHASE_B_INDICATOR_SCOPE_AUTHORITY:BROAD_PACKAGE_SNAPSHOT -->

- Refresh wave: `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23`
- Active packet: `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`
- Indicator artifact: `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`
- Purpose: Phase B mechanically collected and staged this same-wave L4 indicator before review so the tracker note, Gate 8 package, and governing packet describe one staged scope.
- Scope binding: no indicator file other than the artifact above is in scope for this wave.
- Authorized staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_commit_executor_post_merge_cleanup.py`
  - `mu/tests/tools/test_executor_dispatch.py`
  - `mu/tools/executors/commit_executor.py`
  - `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`
  - `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`
<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:end -->

<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:start -->
## Same-Wave Deferred Non-Blocking Authorization

- Refresh wave: `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23`
- Purpose: Phase B and commit automation may stage the same-wave non-blocking bridge findings packet as deferred follow-up instead of blocking an otherwise commit-ready wave.
- Authorized deferred packet(s):
  - `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`
- Scope binding: the packet(s) above are in scope only as generated same-wave non-blocking bridge findings packets.
- Acceptance binding: the final touched-file set may include the packet(s) above when they are also present in `deferred_items` or current staged files.
<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:end -->

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23 --output reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_commit_executor_post_merge_cleanup.py mu/tests/tools/test_executor_dispatch.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package. scope_refs: `TASKS.md`, `mu/tests/tools/test_commit_executor_post_merge_cleanup.py`, `mu/tests/tools/test_executor_dispatch.py`, `mu/tools/executors/commit_executor.py`, `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`, `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`, `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`..
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23`
- Active packet: `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `b8e726678fec22d41dcc18b9bcd31943dc56a48c5eeb53792e9239794f34a0e9`
- Indicator artifact: `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_commit_executor_post_merge_cleanup.py mu/tests/tools/test_executor_dispatch.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package. scope_refs: `TASKS.md`, `mu/tests/tools/test_commit_executor_post_merge_cleanup.py`, `mu/tests/tools/test_executor_dispatch.py`, `mu/tools/executors/commit_executor.py`, `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`, `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`, `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`..
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_commit_executor_post_merge_cleanup.py`
  - `mu/tests/tools/test_executor_dispatch.py`
  - `mu/tools/executors/commit_executor.py`
  - `reports/control_plane/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_2026-08-23.md`
  - `reports/deferred/non_blocking/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/pr1219-p0ibrrcp-prereq-fetched-tree-queue-authority-2026-08-23.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
