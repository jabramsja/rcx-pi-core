# Recovery Scope Drift Exclude Pyc Bytecode 2026-06-09 2026-06-10

Date: 2026-06-10
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: recovery-scope-drift-exclude-pyc-bytecode-2026-06-09
Phase-A-Lock: LOCKED
Purpose: GOAL: Stop the recovery scope-validation (the observed-drift check in recovery_gate) from flagging gitignored __pycache__/**/*.pyc compiled bytecode as "hybrid observed drift escaped declared scope", which loops the diagnose<->delegate recovery (recovered=False) whenever a Step-15 bot-remediation or tier-3 recovery verifies its fix by RUNNING a test (the pytest run regenerates mu/tests/.../__pycache__/*.cpython-NNN.pyc).

## Scope

In-scope edit allowlist (exact paths -- nothing else may be modified by this wave):

- `mu/tools/executors/recovery_gate.py` -- the observed-drift out-of-scope determination (`observed_drift = manifest_reasons | inventory_reasons`); generalize the existing `.scratch/__pycache__` exclusion to all gitignored `__pycache__/**/*.pyc` bytecode.
- `mu/tests/tools/test_recovery_gate.py` -- regression test for the new exclusion (added in-place to the existing module; no new test file).

Generated / governance artifacts (NOT hand-edited by this wave; produced or referenced mechanically):

- `reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json` -- emitted by `collect_l4_wave_indicators.py` (indicator_collection_command), not an edit target.
- `TASKS.md` and this packet -- governance authority, updated only via `tracker_sync_note.py` / the packet rewrite, not as code.

Intent: exclude gitignored `__pycache__/**/*.pyc` bytecode (anywhere in the repo, not just `.scratch/__pycache__`) from recovery_gate's observed-drift scope check, so a test-run-regenerated `.pyc` during a Step-15/tier-3 recovery is not flagged "escaped declared scope" (which loops diagnose<->delegate, recovered=False). Tooling-only L4_ENABLER; no runtime dirs.

## Work Items

1. **Generalize the bytecode exclusion in the observed-drift scope check** (`mu/tools/executors/recovery_gate.py`). Extend the existing narrow `.scratch/__pycache__` exclusion so the observed-drift out-of-scope determination excludes gitignored `__pycache__/**/*.pyc` compiled bytecode anywhere in the repo (e.g. a regenerated `mu/tests/.../__pycache__/*.cpython-NNN.pyc`). Regenerated bytecode must never contribute to the "hybrid observed drift escaped declared scope: <path>" rejection. All other drift detection stays unchanged -- a real source/test change must still be flagged.
2. **Add a regression test** (`mu/tests/tools/test_recovery_gate.py`). Assert (a) a recovery scope-validation whose observed drift includes a regenerated `mu/tests/.../__pycache__/*.cpython-NNN.pyc` does NOT report "escaped declared scope" for that `.pyc`; and (b) a real out-of-scope source change is STILL flagged "escaped declared scope" -- proving the exclusion is bytecode-only, not a blanket loosening of scope validation.

## Constraints

NOT in scope (hard boundaries):

- MUST NOT touch any runtime dir: `mu/host`, `mu/substrate`, `mu/closures`, `mu/bridge`, `mu/programs`, `rcx_pi/selfhost`, `mu/tools/compilers`. This is an L4_ENABLER tooling-only change.
- Do NOT broaden the exclusion to non-bytecode paths. Scope strictly to gitignored `__pycache__` `.pyc` bytecode; do not exempt any other gitignored or out-of-scope path.
- No masking: no retry/skip/xfail, and do NOT weaken, relax, or delete any existing scope-validation test.
- Do NOT alter the diagnose/delegate recovery control flow beyond the scope-check exclusion. The loop fix is a consequence of the exclusion, not a separate control-flow edit.
- No new files. The regression goes into the existing `mu/tests/tools/test_recovery_gate.py`.

## Stop Conditions

Halt and escalate (do not bypass a gate) if any of these is hit:

- The narrow exclusion cannot be implemented without editing a runtime dir -> STOP; the L4_ENABLER class would be violated.
- Excluding bytecode also suppresses flagging of a real out-of-scope source/test change -> STOP; the exclusion is over-broad and must be tightened to bytecode-only.
- Making the regression pass requires broadening the exclusion beyond gitignored `__pycache__/**/*.pyc` -> STOP; scope creep.
- Implementing the change requires modifying or weakening an existing passing scope-validation test -> STOP; masking is forbidden.
- The evidence_command cannot be made green without one of the above violations -> STOP and escalate rather than bypass.

## Acceptance Criteria

- The observed-drift out-of-scope check in `recovery_gate.py` excludes gitignored `__pycache__/**/*.pyc` bytecode anywhere in the repo (generalized from the `.scratch/__pycache__` precedent).
- A regenerated `mu/tests/.../__pycache__/*.cpython-NNN.pyc` in observed drift no longer yields "hybrid observed drift escaped declared scope", so recovery no longer loops diagnose<->delegate on regenerated bytecode.
- A real out-of-scope source change is STILL flagged "escaped declared scope" (real-drift detection unregressed).
- A new regression test in `mu/tests/tools/test_recovery_gate.py` covers BOTH the excluded-`.pyc` case and the still-flagged real-drift case.
- The wave evidence_command passes: `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_recovery_gate.py`.
- No runtime dir touched; no existing test weakened; no retry/skip/xfail introduced.

## Grounding / Authorization

- **TASKS.md authorization:** `TASKS.md` -- Tracker sync note (2026-06-10, recovery-scope-drift-exclude-pyc-bytecode-2026-06-09): **NEXT-CODEX-POST-REDTEAM exclude gitignored __pycache__ bytecode from recovery observed-drift check.** Class: L4_ENABLER. target_gate_id: G8.
- **Same-wave override token (verbatim from TASKS.md):** `FOUNDER_OVERRIDE:recovery-scope-drift-exclude-pyc-bytecode-2026-06-09 (standing pipeline-bug-fix authorization per memory feedback_autonomous_executor_fix; hand-finish of GO'd fix stranded by dispatcher #62 post_reentry re-route loop)`
- **Authorization:** standing pipeline-bug-fix authorization -- this is a pipeline/control-plane recovery-loop bug fix in `recovery_gate.py` (tooling-only, L4_ENABLER), covered by the wave's `FOUNDER_OVERRIDE` token above so commit automation can derive the same-wave override mechanically.
- **Governing packet:** this file (`reports/control_plane/recovery_scope_drift_exclude_pyc_bytecode_2026-06-09_2026-06-10.md`), referenced as the `Packet:` in TASKS.md.
- **L4 class gates (L4_ENABLER):** target_gate_id `G8`; evidence_command `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_recovery_gate.py`; primary_blocker_class `INTEGRATION`; primary_invariant_id `INV_TYPED_FAIL_CLOSED_OUTCOMES`. Full auto-derived field set is in the "Request from Post-Merge Supervisor" block below (single source of truth -- not hand-edited).

## Request from Post-Merge Supervisor

GOAL: Stop the recovery scope-validation (the observed-drift check in recovery_gate) from flagging gitignored __pycache__/**/*.pyc compiled bytecode as "hybrid observed drift escaped declared scope", which loops the diagnose<->delegate recovery (recovered=False) whenever a Step-15 bot-remediation or tier-3 recovery verifies its fix by RUNNING a test (the pytest run regenerates mu/tests/.../__pycache__/*.cpython-NNN.pyc).

CONTEXT (verified on dev): the observed-drift check builds observed_drift = manifest_reasons | inventory_reasons and rejects on the first out-of-scope path with detail "hybrid observed drift escaped declared scope: <path>". The existing .pyc handling only excludes the specific .scratch/__pycache__ path (a narrow case), and the sys.dont_write_bytecode=True guard only suppresses repo-local .pyc writes during the phase_b_implementer module LOAD -- neither covers a pytest run during recovery that regenerates __pycache__/*.cpython-NNN.pyc under mu/tests/ (or elsewhere). So a regenerated test .pyc lands in observed_drift, is judged out-of-scope, and the recovery iteration fails -> re-diagnose -> re-delegate -> same .pyc -> loop.

REQUIRED FIX (narrow): exclude gitignored __pycache__/**/*.pyc compiled bytecode (anywhere in the repo, not just .scratch/__pycache__) from the observed-drift out-of-scope determination -- regenerated bytecode is never a real change. Generalize the existing .scratch/__pycache__ exclusion precedent to all gitignored __pycache__ bytecode in the scope check. Keep all other drift detection unchanged (a real source/test change must still be flagged). Add a regression test: a recovery scope-validation whose observed drift includes a regenerated mu/tests/.../__pycache__/*.cpython-NNN.pyc must NOT report "escaped declared scope" for that .pyc, while a real out-of-scope source change still does.

This is an L4_ENABLER tooling-only change (recovery_gate.py plus its tests): MUST NOT touch any runtime dir (mu/host, mu/substrate, mu/closures, mu/bridge, mu/programs, rcx_pi/selfhost, mu/tools/compilers). No masking (no retry/skip/xfail; do not weaken existing scope-validation tests). Scope strictly to gitignored __pycache__ bytecode; do not broaden the exclusion to non-bytecode paths.

Routed next-candidate:
recovery-scope-drift-exclude-pyc-bytecode-2026-06-09

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id recovery-scope-drift-exclude-pyc-bytecode-2026-06-09 --output reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_recovery_gate.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/recovery_scope_drift_exclude_pyc_bytecode_2026-06-09_2026-06-10.md. (2) Final pytest gate covered 1 test file(s) from the wave-owned diff. (3) Pre-commit supervisor package is staged at .scratch/phase_b_supervisor_package.json; commit handoff receipt remains pending the supervisor decision..
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: recovery-scope-drift-exclude-pyc-bytecode-2026-06-09 (standing pipeline-bug-fix authorization per memory feedback_autonomous_executor_fix; hand-finish of GO'd fix stranded by dispatcher #62 post_reentry re-route loop)
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `recovery-scope-drift-exclude-pyc-bytecode-2026-06-09`
- Active packet: `reports/control_plane/recovery_scope_drift_exclude_pyc_bytecode_2026-06-09_2026-06-10.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `3e9ee9254e2d3e3d7004add02054ab732730f12cb2154b4f7188ea18ab5d89f9`
- Indicator artifact: `reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_recovery_gate.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/recovery_scope_drift_exclude_pyc_bytecode_2026-06-09_2026-06-10.md. (2) Final pytest gate covered 1 test file(s) from the wave-owned diff. (3) Pre-commit supervisor package is staged at .scratch/phase_b_supervisor_package.json; commit handoff receipt remains pending the supervisor decision..
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_recovery_gate.py`
  - `mu/tools/executors/recovery_gate.py`
  - `reports/control_plane/recovery_scope_drift_exclude_pyc_bytecode_2026-06-09_2026-06-10.md`
  - `reports/l4_wave_indicators/recovery-scope-drift-exclude-pyc-bytecode-2026-06-09.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
