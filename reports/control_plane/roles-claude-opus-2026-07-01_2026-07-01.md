# Roles to Claude Opus 4.8 across the pipeline (no codex, no fallback)

Date: 2026-07-01
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: roles-claude-opus-2026-07-01
Phase-A-Lock: LOCKED
Purpose: FOUNDER DIRECTIVE (2026-07-01, architect/non-coder, non-interactive autonomous session): make the Claude family run EVERY pipeline role on the Opus 4.8 max model, with codex in NO active role and NO silent fallback to any other model. This reverses the codex/codex default that PR #1191 preserved; the founder has explicitly re-directed to Claude across the board. Concretely: (1) set the committed executor_config.json role_agents to implementer=claude and reviewer=claude so the derived backends (phase_a_executor, phase_b_executor, bot_remediation, dialectic_executor, post_merge_supervisor) and bridge_reviewers all resolve to claude, and the claude agent's model stays claude-opus-4-8 at effort max; (2) set DEFAULT_EXECUTOR_CONFIG role_agents in executor_common.py to implementer=claude and reviewer=claude so the bare/missing-config fallback also resolves to claude and the DEFAULT-vs-live alignment invariant holds (this is what PR #1191 pinned to codex; flip it to claude); (3) de-brittle any test that hardcodes a specific provider or asserts DEFAULT-equals-live in a codex-specific way (for example the dispatcher-config default-load and bridge-reviewer-override tests) so they derive expectations from the live committed config and assert the DERIVATION invariant (derived backends equal role_agents.implementer, bridge_reviewers equal role_agents.reviewer) rather than a hardcoded provider name. Verify NO codex remains in any active role, backend, bridge_reviewer, or supervisor default. Do NOT add a fallback-model anywhere (fail loud, never silently downgrade to opus-4-8-or-anything). Do NOT touch runtime substrate dirs. Do NOT change any engine or projection logic. Keep the change minimal and config/test-only. This is an L4_ENABLER: it enables every pipeline wave launched off dev to run as Claude, which is the founder's standing operating mode.

## Scope

executor_config.json (role_agents to claude/claude), executor_common.py (DEFAULT_EXECUTOR_CONFIG role_agents to claude/claude), and the minimal set of executor/dispatcher config tests that assert a specific provider or pin the DEFAULT literal to a fixed provider -- specifically `mu/tests/tools/test_executor_config_alignment.py` and `mu/tests/tools/test_executor_dispatch.py`. No runtime substrate, no engine logic, no fallback-model.

Files and surfaces in scope:

- `mu/tools/executors/executor_config.json` -- committed `role_agents` flip to `claude/claude` (via `set_roles.py`).
- `mu/tools/executors/executor_common.py` -- `DEFAULT_EXECUTOR_CONFIG` `role_agents` flip to `claude/claude`.
- `mu/tests/tools/test_executor_config_alignment.py` -- de-brittle the absent-committed-config fallback test (`TestBareFallbackFollowsCommittedRoles::test_truly_absent_committed_config_uses_default_literal`) from hardcoded `codex` to the derivation invariant.
- `mu/tests/tools/test_executor_dispatch.py` -- de-brittle any provider-hardcoded dispatcher/bridge-reviewer config assertion to the derivation invariant.
- TASKS.md -- tracker-sync authority. The 2026-07-01 tracker sync note for wave `roles-claude-opus-2026-07-01` is the single source of truth for this packet's L4 fields; the packet derives from it.

- `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`
  - Same-wave Phase B/commit generated deferred non-blocking bridge findings packet only; no unrelated deferred report is authorized by this wave.

## Work items

1. Flip committed `mu/tools/executors/executor_config.json` `role_agents` to `implementer=claude` and `reviewer=claude` via `set_roles.py` (the single role-switch surface), so the derived backends -- `phase_a_executor`, `phase_b_executor`, `bot_remediation`, `dialectic_executor`, `post_merge_supervisor` -- and `bridge_reviewers` all resolve to `claude`, with the `claude` agent kept at model `claude-opus-4-8`, effort `max`.
2. Flip `DEFAULT_EXECUTOR_CONFIG` `role_agents` in `mu/tools/executors/executor_common.py` to `implementer=claude` and `reviewer=claude` (PR #1191 pinned this to codex) so the bare/missing-config fallback also resolves to `claude` and the DEFAULT-vs-live alignment invariant holds.
3. De-brittle the minimal set of executor/dispatcher config tests that hardcode a specific provider or pin the `DEFAULT_EXECUTOR_CONFIG` `role_agents` literal to a fixed provider, so they derive expectations from the live committed config / the `DEFAULT_EXECUTOR_CONFIG` literal and assert the DERIVATION invariant (derived backends == `role_agents.implementer`; `bridge_reviewers` == `role_agents.reviewer`; `commit_executor` stays unassigned/`None`) rather than a hardcoded provider name. This set MUST include `mu/tests/tools/test_executor_config_alignment.py::TestBareFallbackFollowsCommittedRoles::test_truly_absent_committed_config_uses_default_literal`, which currently pins the absent-committed-config fallback literal to `codex/codex` and asserts that every derived backend and `bridge_reviewer` equals `"codex"`; work item 2 flips that literal to `claude/claude`, so those hardcoded-`codex` assertions go red and MUST be re-expressed against the live literal (derived fields == `default_ra["implementer"]` / `default_ra["reviewer"]`, keeping `commit_executor` unassigned). It also covers the dispatcher-config default-load and bridge-reviewer-override tests in `mu/tests/tools/test_executor_dispatch.py` for any provider-hardcoded assertion the committed flip (work item 1) turns red.

## Constraints

- Config/test-only and minimal: touch only `executor_config.json`, `executor_common.py`, and the affected executor/dispatcher config tests (`mu/tests/tools/test_executor_config_alignment.py`, `mu/tests/tools/test_executor_dispatch.py`). Create NO new files.
- Do NOT touch runtime substrate dirs (`mu/host/`, `rcx_pi/selfhost/`) -- required for an L4_ENABLER (MUST NOT touch runtime dirs).
- Do NOT change any engine or projection logic.
- Do NOT add a fallback-model anywhere: fail loud, never silently downgrade to any other model.
- Do NOT re-introduce codex into any active role, derived backend, `bridge_reviewer`, or supervisor default.

## Stop conditions

- STOP and escalate to the founder if flipping roles cannot be done without touching a runtime substrate dir or engine/projection logic (that would exceed the config/test-only scope).
- STOP if the flip cannot hold without introducing a fallback-model or silent-downgrade path -- fail loud instead of adding one.
- STOP if de-brittling a test would require re-asserting a hardcoded provider, or if a test encodes a genuine codex-only invariant that cannot be re-expressed as the derivation invariant -- escalate rather than delete the assertion.
- STOP if, after the flip, `set_roles.py --show` still reports any codex in an active role -- the acceptance invariant has failed and the root cause is outside the config/test surfaces.

## Validation gates

- evidence_command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_executor_config_alignment.py`

## Acceptance criteria

- `python3 mu/tools/executors/set_roles.py --show` reports `implementer=claude` and `reviewer=claude` (Claude Opus 4.8, effort `max`).
- Committed `executor_config.json` and `DEFAULT_EXECUTOR_CONFIG` `role_agents` are both `claude/claude`; the DEFAULT-vs-live alignment invariant holds.
- Derived backends (`phase_a_executor`, `phase_b_executor`, `bot_remediation`, `dialectic_executor`, `post_merge_supervisor`), `bridge_reviewers`, and the supervisor default all resolve to `claude`.
- `PYTHONHASHSEED=0 python3 -m pytest mu/tests/tools/test_executor_config_alignment.py mu/tests/tools/test_executor_dispatch.py -q` passes fully GREEN (zero failures), with the affected tests -- including `test_executor_config_alignment.py::TestBareFallbackFollowsCommittedRoles::test_truly_absent_committed_config_uses_default_literal` -- deriving expectations from the live config / `DEFAULT_EXECUTOR_CONFIG` literal (derivation invariant) rather than a hardcoded provider name.
- `grep` finds no `codex` in any active role, derived backend, `bridge_reviewer`, or supervisor default.
- No fallback-model / silent-downgrade path is present anywhere in the touched surfaces.

## Grounding / Authorization

- Task: [NEXT-CODEX-POST-REDTEAM]; wave id `roles-claude-opus-2026-07-01`.
- Governing packet: this file, `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`.
- TASKS.md authority: the 2026-07-01 tracker sync note for wave `roles-claude-opus-2026-07-01` is canonical for this packet's L4 fields.
- Authorization: Founder-authorized 2026-07-01: Claude does everything in the pipeline and orchestrator; codex nowhere; model Opus 4.8 max; protect from any fallback. FOUNDER_OVERRIDE:roles-claude-opus-2026-07-01.

FOUNDER_OVERRIDE:roles-claude-opus-2026-07-01

<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:start -->
## Phase B Indicator Scope Reconciliation

- Refresh wave: `roles-claude-opus-2026-07-01`
- Active packet: `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`
- Indicator artifact: `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`
- Purpose: Phase B mechanically collected and staged this same-wave L4 indicator before pre-commit supervisor review so the tracker note, Gate 8 package, and governing packet describe one staged scope.
- Scope binding: no indicator file other than the artifact above is in scope for this wave.
- Authorized staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_executor_config_alignment.py`
  - `mu/tools/executors/executor_common.py`
  - `mu/tools/executors/executor_config.json`
  - `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`
  - `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`
<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:end -->

<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:start -->
## Same-Wave Deferred Non-Blocking Authorization

- Refresh wave: `roles-claude-opus-2026-07-01`
- Purpose: Phase B and commit automation may stage the same-wave non-blocking bridge findings packet as deferred follow-up instead of blocking an otherwise commit-ready wave.
- Authorized deferred packet(s):
  - `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`
- Scope binding: the packet(s) above are in scope only as generated same-wave non-blocking bridge findings packets.
- Acceptance binding: the final touched-file set may include the packet(s) above when they are also present in `deferred_items` or current staged files.
<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:end -->

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id roles-claude-opus-2026-07-01 --output reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_executor_config_alignment.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md. (2) Final pytest gate covered 1 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package. scope_refs: `TASKS.md`, `mu/tests/tools/test_executor_config_alignment.py`, `mu/tools/executors/executor_common.py`, `mu/tools/executors/executor_config.json`, `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`, `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`, `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`..
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: roles-claude-opus-2026-07-01.
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `roles-claude-opus-2026-07-01`
- Active packet: `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `40d79d561051a0cea1dde11d048567f6d1feb064a5e85150d57f21a2f86ed46d`
- Indicator artifact: `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/tools/test_executor_config_alignment.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md. (2) Final pytest gate covered 1 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package. scope_refs: `TASKS.md`, `mu/tests/tools/test_executor_config_alignment.py`, `mu/tools/executors/executor_common.py`, `mu/tools/executors/executor_config.json`, `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`, `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`, `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`..
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/tools/test_executor_config_alignment.py`
  - `mu/tools/executors/executor_common.py`
  - `mu/tools/executors/executor_config.json`
  - `reports/control_plane/roles-claude-opus-2026-07-01_2026-07-01.md`
  - `reports/deferred/non_blocking/roles-claude-opus-2026-07-01_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/roles-claude-opus-2026-07-01.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
