# Structural-Numbers-Add-Js-Parity-2026-06-18 2026-06-18

Date: 2026-06-18
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: structural-numbers-add-js-parity-2026-06-18
Phase-A-Lock: LOCKED
Purpose: StructuralNumbers JS-parity-for-add: L4_ENABLER gate proving the landed ADD projections run in the JS substrate (bootstrap_core via node) content-addressed-equal to the Python run_mu add -- the mandatory L3 parity for the first arithmetic op. JS Mu via container_factory; run_mu/node classes l4_expensive; growth cap pre-bumped. Gate-only, no runtime change.

## Request from Post-Merge Supervisor

StructuralNumbers JS-parity-for-add: L4_ENABLER gate proving the landed ADD projections run in the JS substrate (bootstrap_core via node) content-addressed-equal to the Python run_mu add -- the mandatory L3 parity for the first arithmetic op. JS Mu via container_factory; run_mu/node classes l4_expensive; growth cap pre-bumped. Gate-only, no runtime change.

Routed next-candidate:
structural-numbers-add-js-parity-2026-06-18

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id structural-numbers-add-js-parity-2026-06-18 --output reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package.
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: structural-numbers-add-js-parity-2026-06-18.
<!-- L4_FIELDS_FROM_TRACKER:end -->

## Scope

Files / directories in scope for this wave (additive, gate-only).

**Created or modified by this wave (the complete touch list):**

- `reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md` -- this governing Phase A packet (modified).
- `mu/tests/l4_gates/test_structural_numbers_add_js_parity.py` -- NEW cross-substrate L3 parity gate for binary ADD (the wave's `structural_artifact_ref`). This one new file is also where the `@pytest.mark.l4_expensive` + `@pytest.mark.slow` decorators are applied and where `container_factory` is imported; no separate classification-config or test-harness file is edited.
- `mu/tests/docs/test_growth_caps.py` -- edit the single `CAP_TEST_FILES` constant (136 -> 137) to admit the one new gate file under the test-file growth cap.
- `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json` -- L4 wave indicator artifact, generated by the `indicator_collection_command` for gate evidence.

**Used (imported / read / executed) but NOT modified -- no diff permitted under these paths:**

- `mu/host/js/core/container_factory.js` -- USE-ONLY. The new gate imports this existing, `Object.freeze`d module and calls its already-exported constructors (`list(...)` / `record(...)`, with `has(...)` as the read predicate) to build the trusted JS Mu inputs/outputs it runs through JS `bootstrap_core` (via `node`). The factory adds each constructed value to its private trusted-Mu set at call time -- that is the existing runtime API being exercised, NOT a source edit. The "registered" wording in the tracker note's `evidence_delta` refers to this call-time trusting, not a code change to the file.
- `pyproject.toml` -- the `l4_expensive` and `slow` markers are ALREADY registered here; the new test only applies them as decorators, so this file is NOT edited.
- The already-landed ADD projections themselves: Python `run_mu` add (under `mu/host/python/rcx_pi/selfhost/`) and the JS substrate (`mu/host/js/eval_step.js`) are EXECUTED for the parity comparison, NOT modified.

## Work items

Concrete, bounded tasks for this wave (from the TASKS.md tracker note `evidence_delta` for this wave):

1. Add `mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`. Over a practical ADD corpus, run the SAME add projections through (a) Python `run_mu` add and (b) JS `bootstrap_core` (via `node`); assert the two results are content-addressed-equal (`muHashCached` byte-identical) and that both decode to host `a + b`.
2. In that new gate file, import the existing `mu/host/js/core/container_factory.js` module and call its already-exported `list(...)` / `record(...)` constructors to build the trusted JS Mu values the gate compares. This USES the existing factory API (which trusts each value at call time); it does NOT modify `mu/host/js/core/`.
3. Apply the `@pytest.mark.l4_expensive` + `@pytest.mark.slow` decorators to the run_mu/node parity test(s) in the new gate file so they are excluded from the fast green gate and run under nightly with a 900s timeout. Both markers are ALREADY registered in `pyproject.toml`, so no classification-config file is edited.
4. Pre-bump the `CAP_TEST_FILES` constant in `mu/tests/docs/test_growth_caps.py` (136 -> 137) so the single new gate file is admitted under the test-file growth cap.
5. Collect the L4 wave indicator artifact via the `indicator_collection_command` for gate evidence.

## Constraints

What is explicitly NOT in scope (this is an L4_ENABLER -- gate-only, additive):

- No runtime/substrate change. Do NOT modify runtime dirs -- the Python runtime under `mu/host/python/rcx_pi/selfhost/`, the JS substrate `mu/host/js/eval_step.js`, or the JS core under `mu/host/js/core/` (including `container_factory.js`, which is USE-ONLY: imported and called, never edited). (L4_ENABLER MUST NOT touch runtime dirs.)
- No change to the already-landed Python ADD projections themselves; this wave only validates them cross-substrate.
- No seed change; no ratchet increase; no host-authority / host-semantics increase (`check_host_semantics_ratchet.py` must stay flat).
- Per North Star semantics, do NOT add host-only canonicalization or any host-only semantics to force parity. Parity must hold structurally or be surfaced as a finding.
- Out of scope for THIS wave: compare-op JS parity, codec JS parity, and the Stage-4 cutover. This gate establishes the ADD pattern those follow-ons will reuse.

## Stop conditions

- Phase A boundary: stop after this packet rewrite. Do NOT begin implementation and do NOT inspect/modify downstream implementation files this turn.
- Stop and surface as a finding (do NOT mask) if JS `bootstrap_core` add and Python `run_mu` add are NOT content-addressed-equal over the corpus -- that is a genuine L3 parity divergence; do not paper over it with host-only canonicalization or host-only semantics.
- Stop and escalate to founder if achieving the gate would require touching runtime dirs -- the Python runtime, `mu/host/js/eval_step.js`, or `mu/host/js/core/` (for example, if `container_factory.js`'s existing `list` / `record` / `has` API turns out to be insufficient and a source edit would be needed). That would break the L4_ENABLER class; request reclassification rather than proceeding.
- Stop if the current test-file count is not 136 (the 136 -> 137 pre-bump assumption would be stale); re-derive the cap before changing it.
- Stop if the new parity classes cannot be kept out of the fast green gate (i.e., the 900s run would leak into per-commit / green-gate timing).
- Standard gate discipline: stop on any failure in the `evidence_command` and diagnose; do NOT bypass gates or use `--no-verify`.

## Acceptance criteria

- `evidence_command` passes: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`. (The full l4_expensive cross-substrate parity run + `check_host_semantics_ratchet.py` are covered by the dedicated acceptance bullets below.)
- The gate asserts content-addressed equality (`muHashCached` byte-identical) between JS `bootstrap_core` add and Python `run_mu` add over the corpus, and that both decode to host `a + b`.
- The run_mu/node parity classes carry `l4_expensive` + `slow`; they are excluded from the fast green gate and pass under nightly (900s).
- `CAP_TEST_FILES` == 137 in `mu/tests/docs/test_growth_caps.py` after the bump; the test-file growth-cap check passes.
- `check_host_semantics_ratchet.py` passes -- no host-authority / host-semantics increase.
- Runtime dirs and seeds untouched: NO diff under `mu/host/python/rcx_pi/selfhost/`, `mu/host/js/eval_step.js`, or `mu/host/js/core/` (including `mu/host/js/core/container_factory.js`, which is USE-ONLY). `pyproject.toml` is also unchanged (both markers already registered). `git diff --check` clean.
- L4 wave indicator JSON exists at `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`.
- Net effect equals `progress_proof_after`: binary ADD's L3 parity is locked across Python `run_mu` and JS `bootstrap_core` (the first arithmetic op validated in both substrates; the pattern for compare/codec JS-parity and the Stage-4 cutover).

## Grounding / Authorization

- Task: `[NEXT-CODEX-POST-REDTEAM]` -- StructuralNumbers JS-parity-for-add cross-substrate gate.
- Authorization source: `TASKS.md` line 546 -- Tracker sync note (2026-06-18, structural-numbers-add-js-parity-2026-06-18); Class: L4_ENABLER; target_gate_id: G8.
- Governing packet: this file (`reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md`).
- L4 fields: see the auto-derived `L4_FIELDS_FROM_TRACKER` block above (single source of truth; do not hand-edit).
- Authorization: wave-bound founder override recorded in TASKS.md line 546 (2026-06-18 tracker sync note); Class L4_ENABLER; target_gate_id G8.

Machine-detectable, wave-bound override (commit automation derives the same-wave override from this line):

FOUNDER_OVERRIDE:structural-numbers-add-js-parity-2026-06-18

<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:start -->
## Phase B Indicator Scope Reconciliation

- Refresh wave: `structural-numbers-add-js-parity-2026-06-18`
- Active packet: `reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md`
- Indicator artifact: `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`
- Purpose: Phase B mechanically collected and staged this same-wave L4 indicator before pre-commit supervisor review so the tracker note, Gate 8 package, and governing packet describe one staged scope.
- Scope binding: no indicator file other than the artifact above is in scope for this wave.
- Authorized staged files:
  - `TASKS.md`
  - `mu/tests/docs/test_growth_caps.py`
  - `mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`
  - `reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md`
  - `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`
<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `structural-numbers-add-js-parity-2026-06-18`
- Active packet: `reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `4af8eba2fdad1076f0e856d37b36bc1feb53fd43a18334eedb44b85dedc32db3`
- Indicator artifact: `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package.
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/docs/test_growth_caps.py`
  - `mu/tests/l4_gates/test_structural_numbers_add_js_parity.py`
  - `reports/control_plane/structural-numbers-add-js-parity-2026-06-18_2026-06-18.md`
  - `reports/l4_wave_indicators/structural-numbers-add-js-parity-2026-06-18.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
