# Structural-Numbers-Codec-2026-06-18 2026-06-18

Date: 2026-06-18
Status: IMPLEMENTED / LOCAL EVIDENCE
Task: [NEXT-CODEX-POST-REDTEAM]
Wave ID: structural-numbers-codec-2026-06-18
Phase-A-Lock: LOCKED
Purpose: StructuralNumbers Stage 2c (Python-only codec): L4_ENABLER gate proving the nested<->flat numeral CODEC as RCX projections via run_mu, with round-trip identity (both directions) + valid-Mu + same-host-value anchoring; run_mu classes l4_expensive, growth cap pre-bumped. JS parity deferred (mirrors add/compare).

## Scope

Additive, gate-only, Python-only. Files/directories this wave is allowed to touch:

- `mu/tests/l4_gates/test_structural_numbers_codec.py` — NEW gate test (the `structural_artifact_ref`). Holds the run_mu codec projections and assertions.
- The test-file growth-cap declaration enforced by `mu/tests/docs/test_growth_caps.py` — pre-bump `CAP_TEST_FILES` 135 -> 136 to admit the single new test file.
- `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json` — regenerated by the `indicator_collection_command` (generated artifact, not hand-edited).
- This packet (`reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`) and the canonical TASKS.md tracker note for this wave.

Nothing under runtime/substrate directories is in scope (see Constraints).

- `reports/deferred/non_blocking/structural-numbers-codec-2026-06-18_bridge_nonblockers.md`
  - Same-wave Phase B/commit generated deferred non-blocking bridge findings packet only; no unrelated deferred report is authorized by this wave.

## Work items

Concrete bounded tasks derived from the canonical tracker note's `evidence_delta`:

1. **Author `mu/tests/l4_gates/test_structural_numbers_codec.py`** — a Python `run_mu` gate that expresses the nested<->flat numeral CODEC as RCX projections (linear; no host conversion primitive) and asserts:
   - Forward projection: nested numeral -> flat numeral.
   - Reverse projection: flat numeral -> nested numeral.
   - Round-trip identity in BOTH directions via content-addressed equality (`nested -> flat -> nested == nested` and `flat -> nested -> flat == flat`).
   - Valid-Mu of each converted form.
   - Same-host-value anchoring (the host integer value is preserved across conversion) over a practical corpus.
   - All conversions executed through `run_mu` on the Python substrate only.
2. **Class the run_mu cases** as `@pytest.mark.l4_expensive` + `@pytest.mark.slow` so they are green-gate-excluded and run nightly under the 900s timeout (matches the `--timeout=900` in the evidence command).
3. **Pre-bump the growth cap** `CAP_TEST_FILES` 135 -> 136 (the declaration enforced by `mu/tests/docs/test_growth_caps.py`) so the one new test file keeps the growth ratchet green.
4. **Regenerate the wave indicator artifact** `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json` via the `indicator_collection_command` recorded in the L4 fields block below.

## Constraints

Explicitly NOT in scope (from `evidence_delta` item 3 and the L4_ENABLER class contract in `.claude/rules/l4-contract.md`):

- **No runtime/substrate change.** Do not touch `rcx_pi/selfhost/`, `mu/host/python/rcx_pi/selfhost/`, or `mu/host/js/`. L4_ENABLER MUST NOT touch runtime dirs.
- **No host conversion primitive.** The CODEC must be pure RCX projections run via `run_mu`; adding a host-side nested<->flat converter is forbidden (North Star: no host capabilities in the bootstrap).
- **No seed change** and **no ratchet/authority increase** (host-semantics ratchet must stay flat).
- **No JS parity in this wave.** JS mirror is deferred (mirrors the add/compare staging) and must not be started here.
- **No Stage-4 matcher cutover.** This wave only proves the CODEC as projections; it does not wire it into the matcher.

## Stop conditions

- **Done:** the evidence command exits 0 (the `l4_expensive` pytest passes AND `check_host_semantics_ratchet.py` passes); proceed to the normal Phase B / commit pipeline.
- **STOP & escalate** if the CODEC cannot be expressed without a host conversion primitive (do not add one).
- **STOP & escalate** if any required change would touch a runtime/substrate dir, the seed, or would raise a ratchet/authority count.
- **STOP & trim corpus** if `run_mu` cost makes the corpus exceed the 900s nightly budget — reduce to a lean corpus rather than raising the timeout (run_mu meta-circular cost is high; the add/compare engine facts establish ~0.6s/domain-step, lean-corpus guidance).
- **STOP & revise** (do not commit) if bridge/agent review returns REQUEST_CHANGES.

## Acceptance criteria

- Evidence command green:
  `PYTHONHASHSEED=0 python3 -m pytest -q -m l4_expensive mu/tests/l4_gates/test_structural_numbers_codec.py --timeout=900 --tb=short && python3 mu/tools/checks/check_host_semantics_ratchet.py` exits 0.
- `mu/tests/l4_gates/test_structural_numbers_codec.py` exists; its run_mu cases carry `l4_expensive` + `slow`; they are excluded from the green gate and present in nightly.
- Round-trip identity verified in both directions (content-addressed equal); each converted form verified valid-Mu; same-host-value anchoring verified across the corpus.
- `CAP_TEST_FILES` == 136 and `mu/tests/docs/test_growth_caps.py` passes.
- No diff under runtime/substrate dirs; no seed change; no new host conversion primitive; host-semantics ratchet unchanged.
- JS parity intentionally absent and documented as deferred (mirrors add/compare).
- `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json` regenerated.

## Grounding / Authorization

- **TASKS.md authorization:** Tracker sync note (2026-06-18, structural-numbers-codec-2026-06-18) — Class `L4_ENABLER`, `target_gate_id: G8`, `Packet: reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`, `structural_artifact_ref: mu/tests/l4_gates/test_structural_numbers_codec.py`, with `evidence_command`, `evidence_delta`, and indicator refs as captured verbatim in the L4 fields block below.
- **Governing packet:** this file (`reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`).
- **Wave-bound founder override** (carried verbatim from the canonical tracker note so commit automation derives the same-wave override mechanically): `FOUNDER_OVERRIDE:structural-numbers-codec-2026-06-18`.
- **L4 class contract** (`.claude/rules/l4-contract.md`): L4_ENABLER requires `target_gate_id` + `evidence_command` + `evidence_delta` and MUST NOT touch runtime dirs — all satisfied by the Scope and Constraints above.
- **Predecessor context:** Stage 2a-i ADD and Stage 2b COMPARE are already proven as projections; this wave (Stage 2c CODEC) completes the Stage-2 arithmetic trio (add+compare+codec) before JS parity and the Stage-4 matcher cutover, per the `progress_proof_before` / `progress_proof_after` in the tracker note.

## Request from Post-Merge Supervisor

StructuralNumbers Stage 2c (Python-only codec): L4_ENABLER gate proving the nested<->flat numeral CODEC as RCX projections via run_mu, with round-trip identity (both directions) + valid-Mu + same-host-value anchoring; run_mu classes l4_expensive, growth cap pre-bumped. JS parity deferred (mirrors add/compare).

Routed next-candidate:
structural-numbers-codec-2026-06-18

<!-- L4_FIELDS_FROM_TRACKER:start -->
**L4 fields (auto-derived from the canonical TASKS.md tracker note -- single source of truth; do not hand-edit):**

- `primary_blocker_class`: INTEGRATION.
- `primary_invariant_id`: INV_STRUCTURAL_FORWARD_MOTION.
- `indicator_artifact_ref`: reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json.
- `indicator_collection_command`: python3 mu/tools/metrics/collect_l4_wave_indicators.py --wave-id structural-numbers-codec-2026-06-18 --output reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json.
- `target_gate_id`: G8.
- `evidence_command`: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/l4_gates/test_structural_numbers_codec.py`.
- `evidence_delta`: (1) Phase B converged on the locked plan at reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package.
- `bootstrap_endgame_policy`: SUBSTRATE_INDEPENDENT_MINIMAL_BOOTSTRAP.
- `boot0_track_id`: V1.
- `boot0_progress_state`: HOLD.
- `founder_override`: structural-numbers-codec-2026-06-18.
<!-- L4_FIELDS_FROM_TRACKER:end -->

<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:start -->
## Phase B Indicator Scope Reconciliation

- Refresh wave: `structural-numbers-codec-2026-06-18`
- Active packet: `reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`
- Indicator artifact: `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json`
- Purpose: Phase B mechanically collected and staged this same-wave L4 indicator before pre-commit supervisor review so the tracker note, Gate 8 package, and governing packet describe one staged scope.
- Scope binding: no indicator file other than the artifact above is in scope for this wave.
- Authorized staged files:
  - `TASKS.md`
  - `mu/tests/docs/test_growth_caps.py`
  - `mu/tests/l4_gates/test_structural_numbers_codec.py`
  - `reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`
  - `reports/deferred/non_blocking/structural-numbers-codec-2026-06-18_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json`
<!-- PHASE_B_INDICATOR_SCOPE_REFRESH:end -->

<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:start -->
## Same-Wave Deferred Non-Blocking Authorization

- Refresh wave: `structural-numbers-codec-2026-06-18`
- Purpose: Phase B and commit automation may stage the same-wave non-blocking bridge findings packet as deferred follow-up instead of blocking an otherwise commit-ready wave.
- Authorized deferred packet(s):
  - `reports/deferred/non_blocking/structural-numbers-codec-2026-06-18_bridge_nonblockers.md`
- Scope binding: the packet(s) above are in scope only as generated same-wave non-blocking bridge findings packets.
- Acceptance binding: the final touched-file set may include the packet(s) above when they are also present in `deferred_items` or current staged files.
<!-- SAME_WAVE_DEFERRED_NON_BLOCKING_AUTH:end -->

<!-- COMMIT_PATH_TRUTH_REFRESH:start -->
## Commit Path Truth Refresh

- Refresh wave: `structural-numbers-codec-2026-06-18`
- Active packet: `reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`
- Commit status: `pre_commit_supervisor_pending`
- Tracker note sha256: `bdbef6ba3fae96d3e5c15a3f49163807875eb6ed4be5361ecd96d60e6f927902`
- Indicator artifact: `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json`
- Evidence command: `PYTHONHASHSEED=0 python3 -m pytest -x --tb=short mu/tests/docs/test_growth_caps.py mu/tests/l4_gates/test_structural_numbers_codec.py`.
- Evidence delta: (1) Phase B converged on the locked plan at reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md. (2) Final pytest gate covered 2 test file(s) from the wave-owned diff. (3) Pre-commit supervisor receipt remains pending for the current staged package.
- Evidence handles:
  - `indicator`: `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json`
- Current staged files:
  - `TASKS.md`
  - `mu/tests/docs/test_growth_caps.py`
  - `mu/tests/l4_gates/test_structural_numbers_codec.py`
  - `reports/control_plane/structural-numbers-codec-2026-06-18_2026-06-18.md`
  - `reports/deferred/non_blocking/structural-numbers-codec-2026-06-18_bridge_nonblockers.md`
  - `reports/l4_wave_indicators/structural-numbers-codec-2026-06-18.json`
<!-- COMMIT_PATH_TRUTH_REFRESH:end -->
