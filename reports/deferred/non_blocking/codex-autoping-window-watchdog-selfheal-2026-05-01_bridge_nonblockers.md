# Deferred Non-Blocking Findings: codex-autoping-window-watchdog-selfheal-2026-05-01

Wave: codex-autoping-window-watchdog-selfheal-2026-05-01
Class: L4_ENABLER
Target Gate: G8
Status: RESOLVED_IN_FOLLOWUP
Generated by Phase B executor. 2 finding(s).

## 1. Stale active-ping cleanup is still PID/PGID-only and has a PID-reuse hardening gap
- **Class:** DEFECT
- **Severity:** medium
- **File:** mu/tools/session/codex_autoping_window.sh
- **Disposition:** non_blocking
- **Evidence:** `git show :mu/tools/session/codex_autoping_window.sh | nl -ba | sed -n '204,224p'`

## 2. Cleanup failure is logged but not surfaced in durable autoping state
- **Class:** DEFECT
- **Severity:** low
- **File:** mu/tools/session/codex_autoping_window.sh
- **Disposition:** non_blocking
- **Evidence:** `git show :mu/tools/session/codex_autoping_window.sh | nl -ba | sed -n '288,305p'`

## Follow-up closeout: codex-autoping-active-ping-cleanup-hardening-2026-05-05

Status: RESOLVED.

Implementation evidence:
- Reproduced the active-ping cleanup gap before editing: the wrapper read `active_pid` from durable state and treated the same stale value as both PID and PGID cleanup target. Current repair verifies `last_dispatched_pid`, `last_dispatched_at`, `updated_at`, same autoping log directory, active log existence, and process-group identity before signaling; root and `mu/` paths are the same hardlinked wrapper content.
- Reproduced cleanup-failure surfacing as partially stale: permission and still-alive failures were already durable, but unsafe target/freshness skips were not. Current repair persists unsafe target verification failures as `status=watcher_restart_degraded_active_ping_cleanup_failed` with `last_active_cleanup_error`.
- Focused validation passed: `bash -n tools/session/codex_autoping_window.sh`, `bash -n mu/tools/session/codex_autoping_window.sh`, `python3 -m py_compile mu/tests/tools/test_codex_autoping_watch.py`, and `PYTHONHASHSEED=0 python3 -m pytest -q mu/tests/tools/test_codex_autoping_watch.py::test_autoping_window_restarts_dead_watcher_without_manual_preflight mu/tests/tools/test_codex_autoping_watch.py::test_autoping_window_skips_stale_active_pid_and_records_degraded_state`.
