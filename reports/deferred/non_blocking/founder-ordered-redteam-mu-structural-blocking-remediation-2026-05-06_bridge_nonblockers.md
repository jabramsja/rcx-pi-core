# Deferred Non-Blocking Findings: founder-ordered-redteam-mu-structural-blocking-remediation-2026-05-06

Wave: founder-ordered-redteam-mu-structural-blocking-remediation-2026-05-06
Class: L4_STRUCTURAL
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 1 retained finding(s).

2026-05-10 triage evidence refresh:

- Current JS validation still relies on host reflective shape checks in both
  validation paths: `mu/host/js/core/types.js:116` through
  `mu/host/js/core/types.js:158` and `mu/host/js/core/types.js:176` through
  `mu/host/js/core/types.js:220` wrap prototype/key/descriptor inspection in
  fail-closed `try` blocks.
- Direct transparent Proxy probe exits 0 with `plainValid:true`,
  `proxyValid:true`, and successful `muHashCached(transparentProxy)`, proving
  transparent Proxy rejection is not code-closed by current structural
  reflection.
- `TASKS.md` records the blocking remediation as merged by PR #912 and
  explicitly preserves only hostile/trapping Proxy coverage; transparent Proxy
  rejection still requires a structural Mu provenance rule or an explicit
  host-oracle authorization.
- Current governing route:
  `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`
  as a separate bounded `/mu` structural packet.

2026-05-11 reconciliation:

- **Outcome:** retained live advisory.
- **Governing route:** `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`.
- **Current proof gap:** the routed Phase A packet selected structural
  provenance / boundary normalization, but no successor implementation packet
  has been locked. Current JS still accepts transparent `new Proxy({a: 1}, {})`
  values through validation and hashing as plain Mu records.
- **Hard stop before implementation:** this deferred packet and the completed
  Phase A policy packet authorize no runtime, hash, validator, Stage0, seed,
  scheduler, registry, parity, or production `/mu` edits. A successor packet
  must enumerate exact implementation files and validation.
- **Doctrine boundary:** future work must narrow host bootstrap debt through
  Mu-aligned structural provenance or boundary normalization; it must not add
  semantic host debt through `util.types.isProxy` or an equivalent JavaScript
  host-object oracle without explicit founder authorization.

## 1. Transparent JavaScript Proxy rejection needs a structural provenance rule or explicit host-oracle override
- **Class:** POLICY_BOUND
- **Severity:** medium
- **File:** mu/host/js/core/types.js
- **Disposition:** non_blocking
- **Evidence:** pre-push purity gate rejected the same-wave `require('util')` proxy-oracle import as a new JavaScript kernel stdlib import; pure structural reflection cannot distinguish `new Proxy({a: 1}, {})` from its target without invoking a host detector.
- **Boundary:** hostile/trapping proxy inspection fails closed in the active blocking wave. Fully transparent proxy rejection is deferred until a structural Mu provenance boundary or explicit founder override authorizes a host oracle.
- **Governing route:** `reports/control_plane/transparent_js_proxy_provenance_boundary_2026-05-09.md`
