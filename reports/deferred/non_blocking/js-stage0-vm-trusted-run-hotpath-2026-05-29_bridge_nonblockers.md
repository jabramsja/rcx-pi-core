# Deferred Non-Blocking Findings: js-stage0-vm-trusted-run-hotpath-2026-05-29

Wave: js-stage0-vm-trusted-run-hotpath-2026-05-29
Class: L4_STRUCTURAL
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 1 finding(s).

## 1. stepKernel collapses falsy malformed top-level vmConfig values to null before validation
- **Class:** DEFECT
- **Severity:** low
- **File:** mu/host/js/engine/kernel.js
- **Disposition:** non_blocking
- **Evidence:** `node - <<'NODE'
const { stepKernel } = require('./mu/host/js/engine/kernel');
const muContainers = require('./mu/host/js/core/container_factory');
function trustMu(value) { if (Array.isArray(value)) return muContainers.list(value.map(trustMu)); if (value !== null && typeof value === 'object') return muContainers.record(Object.keys(value).map(key => [key, trustMu(value[key])])); return value; }
try { const packet = stepKernel([], trustMu({ hello: 'world' }), trustMu([{ pattern: { var: 'x' }, body: { var: 'x' } }]), { maxSteps: 1, vmConfig: false, returnPacket: true }); console.log(JSON.stringify({ok: true, kind: packet.kind, termination_reason: packet.result && packet.result.termination_reason})); } catch (e) { console.log(JSON.stringify({ok: false, message: e.message})); }
NODE`
