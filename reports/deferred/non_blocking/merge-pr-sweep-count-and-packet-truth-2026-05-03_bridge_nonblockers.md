# Deferred Non-Blocking Findings: merge-pr-sweep-count-and-packet-truth-2026-05-03

Wave: merge-pr-sweep-count-and-packet-truth-2026-05-03
Class: L4_ENABLER
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 3 finding(s).

## 1. Invalid MERGE_PR_SWEEP_COUNT aborts target-only merge mode before any sweep is requested
- **Class:** DEFECT
- **Severity:** low
- **File:** mu/tools/hooks/merge_pr.sh
- **Disposition:** non_blocking
- **Evidence:** `git show :mu/tools/hooks/merge_pr.sh | nl -ba | sed -n '245,296p'; set +e; MERGE_PR_SWEEP_COUNT=abc bash mu/tools/hooks/merge_pr.sh 860 2>&1; rc=$?; printf 'exit=%s\n' "$rc"`

## 2. Mechanize L4 target_gate_id normalization for generated packet and tracker metadata
- **Class:** DEFECT
- **Severity:** medium
- **Disposition:** non_blocking follow-up; immediate staged metadata was repaired to `G8` and `python3 tools/checks/enforce_l4_execution_contract.py --staged --wave-id merge-pr-sweep-count-and-packet-truth-2026-05-03` passed after the repair.
- **Evidence:** The pre-commit supervisor rejected the package with `L4_ENABLER missing target_gate_id in tracker note`; the validator only recognizes gate IDs matching `G[0-9]+` at `tools/checks/enforce_l4_execution_contract.py:134` and emits the L4_ENABLER missing-target error at `tools/checks/enforce_l4_execution_contract.py:1809`.
- **Required follow-up:** Trace the packet/tracker metadata source and add a builder or recovery repair so a generated L4 packet cannot use the wave id where a `G1`-`G8` target gate is required.

## 3. Prevent resolved sweep findings from being re-injected into later commit Step 15
- **Class:** DEFECT
- **Severity:** medium
- **Disposition:** non_blocking follow-up; immediate local meta state was cleared after GraphQL confirmed the stored PR #860 review thread is resolved.
- **Evidence:** `.agent_bus/meta/sweep_findings.json` contained thread `PRRT_kwDOQvy8bs5_OcQg`; `gh api graphql -f id=PRRT_kwDOQvy8bs5_OcQg` returned `"isResolved":true`; `commit_executor.py:5972-5991` injects every non-empty line from that local file into Step 15 bot findings.
- **Required follow-up:** Make `merge_pr.sh` or `commit_executor.py` mechanically drop resolved sweep findings, or store enough state to avoid reusing stale local findings after a remediation wave has addressed them.
