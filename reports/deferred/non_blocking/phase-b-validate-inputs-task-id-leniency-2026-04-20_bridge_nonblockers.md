# Deferred Non-Blocking Findings: phase-b-validate-inputs-task-id-leniency-2026-04-20

Wave: phase-b-validate-inputs-task-id-leniency-2026-04-20
Class: unknown
Target Gate: unknown
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 3 finding(s).

## 1. Header-only task extraction is not backward-compatible with the still-queued bold-metadata packet format
- **Class:** DEFECT
- **Severity:** medium
- **File:** mu/tools/executors/phase_b_executor.py
- **Disposition:** non_blocking
- **Evidence:** `python3 - <<'PY'
from pathlib import Path
from mu.tools.executors.phase_b_executor import load_plan_packet
repo = Path('.')
queued = load_plan_packet(repo, 'reports/control_plane/wave1b_pipeline_cleanup_2026-03-31.md')
print(queued.get('task_id'), queued.get('status'))
PY`

## 2. Re-entry deferred non-blocker packets still drop known wave metadata
- **Class:** DOC_ACCURACY
- **Severity:** low
- **File:** mu/tools/executors/phase_b_executor.py
- **Disposition:** non_blocking
- **Evidence:** `python3 - <<'PY'
from pathlib import Path
from tempfile import TemporaryDirectory
from mu.tools.executors.phase_b_executor import _sync_deferred_non_blocking_state
findings=[{'class':'DOC_ACCURACY','severity':'low','title':'t','file':'f','line_start':1,'line_end':1,'evidence_cmd':'echo','evidence_result':'x','status':'new','disposition':'non_blocking'}]
with TemporaryDirectory() as td:
    tmp = Path(td)
    _, rel = _sync_deferred_non_blocking_state(tmp, 'wave-x', [], findings, previous_packet_path=None, executor_created=set())
    print((tmp/rel).read_text().splitlines()[3:5])
PY`

## 3. The staged deferred non-blocker packet is carrying stale review residue, not current review truth
- **Class:** DOC_ACCURACY
- **Severity:** low
- **File:** reports/deferred/non_blocking/phase-b-validate-inputs-task-id-leniency-2026-04-20_bridge_nonblockers.md
- **Disposition:** non_blocking
- **Evidence:** `python3 - <<'PY'
from pathlib import Path
from tempfile import TemporaryDirectory
from mu.tools.executors.phase_b_executor import load_plan_packet, validate_inputs, PhaseBExecutorError
repo = Path('.')
plan = load_plan_packet(repo, 'reports/control_plane/phase_b_validate_inputs_task_id_leniency_2026_04_20_2026-04-21.md')
routing = {'decision':'ROUTE_PHASE_B','summary':'probe','task_id':'[PIPELINE-RECOVERY]','wave_name':'phase-b-validate-inputs-task-id-leniency-2026-04-20','next_candidates':[{'candidate':'phase-b-validate-inputs-task-id-leniency-2026-04-20','bounded':True,'tracked_packet':'reports/control_plane/phase_b_validate_inputs_task_id_leniency_2026_04_20_2026-04-21.md'}]}
validate_inputs(routing, plan)
print('staged_packet=PASS')
with TemporaryDirectory() as td:
    tmp = Path(td)
    (tmp/'reports').mkdir()
    (tmp/'reports'/'plan.md').write_text('Status: ACTIVE\nPhase-A-Lock: LOCKED\nTask: [TASK-A]\nWave ID: wave-a\nTask: [TASK-B]\nWave ID: wave-b\n', encoding='utf-8')
    dup_plan = load_plan_packet(tmp, 'reports/plan.md')
    try:
        validate_inputs({'decision':'ROUTE_PHASE_B','summary':'dup','task_id':'[TASK-A]'}, dup_plan)
        print('duplicate_headers=PASS_UNEXPECTED')
    except PhaseBExecutorError as exc:
        print('duplicate_headers=FAIL_CLOSED', exc)
PY`
