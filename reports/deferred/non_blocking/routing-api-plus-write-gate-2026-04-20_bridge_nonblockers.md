# Deferred Non-Blocking Findings: routing-api-plus-write-gate-2026-04-20

Wave: routing-api-plus-write-gate-2026-04-20
Class: L4_ENABLER
Target Gate: G8
Status: DEFERRED_NON_BLOCKING
Generated by Phase B executor. 1 finding(s).

## 1. Routing-record builder still accepts a directory as `tracked_packet`
- **Class:** DEFECT
- **Severity:** medium
- **File:** mu/tools/executors/executor_common.py
- **Disposition:** non_blocking
- **Evidence:** `python3 - <<'PY'
from pathlib import Path
import os, shutil, subprocess, json
from mu.tools.executors.executor_common import build_post_merge_routing_record
root = Path('.scratch/review_builder_dir_case')
shutil.rmtree(root, ignore_errors=True)
root.mkdir(parents=True)
env = {**os.environ, 'GIT_AUTHOR_NAME':'t', 'GIT_AUTHOR_EMAIL':'t@t', 'GIT_COMMITTER_NAME':'t', 'GIT_COMMITTER_EMAIL':'t@t'}
subprocess.run(['git','init'], cwd=root, capture_output=True, check=True, env=env)
subprocess.run(['git','config','user.name','t'], cwd=root, capture_output=True, check=True)
subprocess.run(['git','config','user.email','t@t'], cwd=root, capture_output=True, check=True)
(root/'reports'/'control_plane'/'dir_packet').mkdir(parents=True)
(root/'reports'/'control_plane'/'seed.md').write_text('# seed\n', encoding='utf-8')
subprocess.run(['git','add','reports/control_plane/seed.md'], cwd=root, capture_output=True, check=True, env=env)
subprocess.run(['git','commit','-m','init'], cwd=root, capture_output=True, check=True, env=env)
record, errors = build_post_merge_routing_record(wave_name='wave', task_id='task', tracked_packet='reports/control_plane/dir_packet', request_for_claude='req', summary='sum', repo_root=root)
print(json.dumps({'errors': errors, 'accepted': errors == [], 'next_candidates': record.get('next_candidates', [])}, indent=2))
PY`
